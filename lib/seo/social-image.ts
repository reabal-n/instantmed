/** Explicit fallback for pages that override the root Open Graph metadata. */
export const DEFAULT_SOCIAL_IMAGE = {
  url: "https://instantmed.com.au/opengraph-image",
  width: 1200,
  height: 630,
  alt: "InstantMed - Online Doctor Australia",
}
