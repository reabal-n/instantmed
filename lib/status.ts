// Re-export from canonical location — do NOT add types here.
// This file exists for backward compatibility only.
export {
  getStatusColor,
  INTAKE_STATUS,
  type IntakeStatus,
  PAYMENT_STATUS,
  type PaymentStatus,
} from "@/lib/data/status"
