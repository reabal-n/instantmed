# AGENTS.md — InstantMed

> Australian telehealth platform for specialised one-off services: medical certificates, repeat prescriptions, hair loss, ED, and women's health (UTI + new/switch contraceptive pill, launched 2026-06-15). Weight loss and general consult remain reserved/retired surfaces gated off until launch readiness is explicitly changed. Patients start with a secure clinical form. A doctor reviews, approves/declines, contacts the patient if clinically needed, and delivers documents or eScript tokens digitally.

> **This file is the project brain.** Always load first. Satellite docs below for deep dives.

## Platform Identity

| Field | Value |
|-------|-------|
| Domain | `instantmed.com.au` |
| Entity | InstantMed Pty Ltd |
| ABN | 64 694 559 334 |
| Address | Level 1/457-459 Elizabeth Street, Surry Hills NSW 2010 |
| Support | support@instantmed.com.au · 0450 722 549 |
| Complaints | complaints@instantmed.com.au · 24h ack · 14-day clinical SLA · canonical page at `/complaints` · linked from footer + Terms §13. Escalation: AHPRA + 8 state HCCC bodies + OAIC (privacy). |
| Doctor model | Supports multiple AHPRA-registered doctors. Per-doctor capability flags on `profiles` (`can_review_med_certs`, `can_review_repeat_rx`, `can_review_consults`, `can_review_ed`, `can_review_hair_loss`, `can_prescribe_s4`, `can_prescribe_s8`) scope future hires before they are verified for a service line; owner-operator is unrestricted. Public surfaces must not disclose doctor count or individual doctor names. Use "AHPRA-registered doctors" for service copy, or "AHPRA-registered Medical Director" only where a governance role is necessary. Never advertise FRACGP, peer review across a cohort, or team training unless formally verified. |
| Staff roles | `admin` (owner-operator only, full clinical + ops + config), `doctor` (clinical only), `support` (non-clinical ops: payment recovery, webhook retries, identity chase-ups, masked PHI). One admin. Multiple doctors and support staff allowed. Capabilities helper at `lib/auth/staff-capabilities.ts`. |
| Hours | The service operates 24/7 (operator decision 2026-07-03): requests submit AND review around the clock for every pathway — med-cert auto-approval runs at all hours; Rx/consult reviews run 7 days with variable timing. Public copy says "24/7" calmly (not plastered) and NEVER states a review-hours window (the old "8am–10pm AEST review hours" framing is retired — `lib/__tests__/hours-copy-contract.test.ts` blocks it) or a customer-facing SLA guarantee. Internal target 1-2h review, 24h max. Never hard-block checkout by time of day. |
| Eligibility | Australia only · strictly 18+ for every paid service (no parental/guardian-consent exception for minors) · Medicare optional for med certs, required for Rx/consults |

## Satellite Documentation

| File | Scope | When to load |
|------|-------|-------------|
| `wiki/index.md` | First navigation layer after the project brain: context map, file directory, architecture snapshot, hygiene audit, refactor plan, decisions, and open questions | After loading the project brain for any non-trivial task, before scanning broad folders |
| `docs/AI_ONBOARDING.md` | **Start here.** 5-min orientation for AI assistants on UI: top 10 rules, canonical files, common gotchas, ship checklist | **First** UI session in any new context. |
| `docs/BUSINESS_PLAN.md` | **Sole owner of durable business strategy:** positioning, active/gated service boundaries, one-off model, owner-doctor model, and expansion exclusions | Strategy, positioning, service/business-model decisions |
| `docs/REVENUE_MODEL.md` | **Sole owner of revenue economics:** `$2k -> $5k -> $10k` milestones, net-retained revenue, first-order contribution, paid-scaling gates, and hiring/capacity triggers | Revenue targets, pricing economics, growth gates, dashboards, staffing assumptions |
| `docs/ADVERTISING_COMPLIANCE.md` | Google Ads, LegitScript, AHPRA/Medical Board, TGA, paid landing page, and audience rules | Ads, landing pages, acquisition copy, metadata, schema |
| `docs/SEO_CONTENT_POLICY.md` | Organic educational content rules, prescription-page guardrails, guide-only article rules, CTA and URL rules | SEO pages, medicine pages, condition/symptom content, health guides |
| `docs/ARTICLE_TEMPLATE.md` | Canonical health-guide authoring standard: page archetypes, body structure + component caps, anti-slop rules, visual system (renderer selection, label budgets, style direction), per-page workflow, GSC-ordered queue, indexing gate | **Every** guide build/deepen/edit in `content/blog/`. Pair with SEO_CONTENT_POLICY.md (compliance law). |
| `docs/BRAND.md` | Brand thesis, patient archetype, voice/personality, tagline system, signature devices (live wait counter, doctor signature, "what we won't do" page, name-first emails, "while you wait" specificity), brand stretch rules. Pinned at v1.0.0. | **Every brand or marketing session.** Load before any tagline, headline, copy sweep, hero, or brand-creative work. Umbrella that points to VOICE / DESIGN / PHOTOGRAPHY_BRIEF for deeper layers. |
| `docs/VOICE.md` | 4-layer brand-string system, dos/don'ts, banned phrases, voice-by-surface rendering, healthcare compliance copy. Code source of truth in `lib/marketing/voice.ts`. CI-enforced. | Any copy work, headline, microcopy, ad creative, or voice review. Pair with BRAND.md. |
| `docs/PHOTOGRAPHY_BRIEF.md` | 8-shot launch list, acceptance criteria, GPT prompt scaffold, cost benchmarks, no-stock-photo rules. | Generating, commissioning, or replacing imagery on marketing pages. |
| `PRODUCT.md` | Design context: user archetypes, brand personality, aesthetic direction, design principles. The "why" behind `DESIGN.md`. CI drift-checked. | Brand/design strategy, voice/aesthetic refresh, before large UI direction shifts. |
| `DESIGN.md` | Color, typography (Source Sans 3 body + Plus Jakarta Sans display), spacing, components, elevation, layout, motion, animation. Pinned at v1.0.0. | **Every UI session.** Load before any UI/frontend/marketing work. The design system is law. |
| `docs/DESIGN_SYSTEM_CHANGELOG.md` | Breaking + notable design-system changes with version history | Bumping `DESIGN_SYSTEM_VERSION` or auditing drift |
| `docs/ARCHITECTURE.md` | System design, data flows, portals, DB schema, API routes, directory index, tech stack, key pages, AI config | Building features, understanding flows, navigating the codebase |
| `docs/CLINICAL.md` | Clinical boundaries, prescribing rules, AI limits, consent, privacy (APP 1-13) | Any clinical logic, AI prompts, compliance work |
| `docs/SECURITY.md` | PHI encryption, RLS, rate limiting, audit logging, incident classification | Security work, auth, data access patterns |
| `docs/OPERATIONS.md` | Incident response, key rotation, debugging, cron jobs, monitoring, env vars, and the Google Ads daily approval/mutation workflow | Ops tasks, debugging, deployments, live Ads account changes |
| `docs/TESTING.md` | Unit test conventions, E2E patterns, auth bypass, coverage rules, CI pipeline | Writing tests, debugging test failures |
| `docs/PRIMITIVES.md` | Marketing primitives registry: social proof, trust badges, stats, pricing, FAQ data, wait times | Adding/changing marketing page content, stats, badges, or FAQ |
| `docs/ROADMAP.md` | **Sole active priority queue:** current operating phase, ordered work, status, checkpoints, and operator cadence | Every next-step or prioritisation question; refresh when status/order changes and at least monthly |
| `docs/DOCTOR_ONBOARDING.md` | Technical onboarding for new clinicians: capability flags, AHPRA, Parchment linking, identity gates, service-line verification | Onboarding a new doctor, capability change, Parchment integration debugging |

---

## Quick Start

```bash
pnpm install
pnpm dev              # Dev server (localhost:3060)
pnpm build            # Production build (8GB heap)
pnpm typecheck        # Type-check without emitting
pnpm lint             # ESLint (flat config)
pnpm test             # Vitest unit tests
pnpm content:audit    # Health guide/content QA backlog
pnpm e2e              # Playwright E2E (needs PLAYWRIGHT=1)
pnpm ci               # Full CI: install → lint → test → build
```

- Unit tests: `lib/__tests__/**/*.test.ts` — Node environment, not jsdom. Coverage: 80% statements, 70% branches, 80% functions, 80% lines (scoped to `lib/clinical/`, `lib/security/`, the `lib/stripe/` payment-safety surface, and `lib/data/intake-lifecycle.ts`; E2E-only Stripe orchestrators — `checkout.ts`/`guest-checkout.ts`/`stripe-session.ts`/`persistence.ts`/`retry-payment.ts` etc. — are excluded in `vitest.config.ts`; `lib/state-machine/` was removed 2026-04-08).
- E2E tests: `e2e/**/*.spec.ts` — auto-seeds/tears down test data. Auth bypass: `PLAYWRIGHT=1` + `__e2e_auth_user_id` cookie.
- Dedicated local app port: `3060`. Use `http://localhost:3060` for manual/Codex Browser Use checks. Moirai uses `3010`; Reabal uses `3055`; do not reuse those ports for InstantMed.

### Browser verification — three approved paths, pick by job

All three are sanctioned for this repo. **agent-browser is explicitly allowed here alongside the `preview_*` tools and Playwright** — this overrides the generic "use `preview_*` only / never drive a browser from Bash" default. Match the tool to the job:

- **`preview_*` tools** — default in-session quick check. Harness-integrated, manages the dev server, knows port `3060`. Best for: confirm a change renders, read console/network/server logs, capture a screenshot as proof. Reach here first for a simple "did it render".
- **agent-browser** (global CLI + skill) — approved for richer interactive verification: drive the real app (`agent-browser open http://localhost:3060` → `snapshot -i` → `click`/`fill` → `screenshot`), step through a full intake/checkout flow, exploratory QA, ref-based a11y assertions. Context-efficient. Needs the dev server already running (`preview_start` or `pnpm dev`).
- **Playwright** (`pnpm e2e`, `PLAYWRIGHT=1`) — the committed E2E suite + CI release gate. Durable specs that own auth bypass, seeded data, and the no-PHI-prefetch network checks — not for ad-hoc "did my change work" loops.

## InstantMed Workflow Skills

Use these repo-specific skills automatically when the task matches. The canonical source lives in `.agent-skills/`; `/Users/rey/.claude/skills` and `/Users/rey/.agents/skills` are generated install targets so Claude Code and Codex can both discover them. After changing any InstantMed skill, run `scripts/sync-agent-skills.sh`, then `scripts/sync-agent-skills.sh --check`. Treat safety, payment, incident, and doc-drift workflows as mandatory before edits; UI and marketing workflows are mandatory before final sign-off; trivial typo fixes and read-only explanation can use them as advisory routing.

| Task trigger | Skill | Gate |
|--------------|-------|------|
| Clinical policy, intake safety, service launch/gating, consult subtypes, repeat Rx, women's health, ED, hair loss, med cert safety, triage, AI drafting, doctor review surfaces | `instantmed-clinical-safety-review` | Mandatory before edits |
| Stripe checkout, guest checkout, retry payment, payment state, webhooks, refunds, decline refunds, price mapping, checkout recovery, intake lifecycle payment transitions | `instantmed-checkout-payment-review` | Mandatory before edits |
| Public copy, homepage/service pages, SEO guides, ads, metadata, schema, claims, pricing copy, testimonials/reviews/social proof, email marketing | `instantmed-marketing-compliance-review` | Mandatory before final sign-off |
| UI, UX, layout, motion, Tailwind, shadcn components, patient flows, marketing pages, staff cockpit, screenshots, browser or Playwright visual proof | `instantmed-ui-browser-verification` | Mandatory before final sign-off |
| Production issue, incident, outage, stuck intake, failed payment, webhook DLQ, Sentry/Vercel/PostHog/Supabase/Stripe/Resend/Parchment recovery | `instantmed-production-incident-review` | Mandatory before edits or recovery |
| Stale docs, source-of-truth drift, `doc:audit`, `CLAUDE.md`/`AGENTS.md`, wiki, architecture/clinical/ops/testing docs, model/version drift | `instantmed-doc-drift-repair` | Mandatory before edits |

### Correction Ladder

When an agent makes a mistake or the user corrects course, persist the learning at the strongest useful layer:

1. **Contract test** — for mechanically checkable failures, such as code/docs model-version drift, route aliases, webhook dispatch parity, or status-transition rules.
2. **Canonical doc update** — for durable product, clinical, privacy, operations, pricing, architecture, advertising, or launch-policy truth.
3. **Skill update** — for workflow failures, such as missing guest checkout, retry-payment, doctor-surface, or browser-verification coverage in a repeated review path.
4. **Memory only** — for personal preference or cross-repo working style that is not enforceable in this codebase, such as proof-scoping language.
5. **No persistence** — for one-off context, temporary debugging notes, or assumptions that were useful only inside the current thread.

## Tech Stack

Next.js 15.5 App Router (webpack) · React 18.3 · TypeScript 5.9 (strict) · Tailwind v4 · Supabase PostgreSQL + Auth · Node 24 LTS · Vercel Pro · Stripe v22 payments · Resend email · PostHog analytics · Sentry errors · Upstash Redis rate limiting · AI SDK (Anthropic + OpenAI) · Framer Motion v11

---

## ⛔ Stack Pin Policy — DO NOT UPGRADE WITHOUT EXPLICIT USER APPROVAL

> **Read this before touching `package.json`, `pnpm-lock.yaml`, or running `pnpm add` / `pnpm update` on any framework dep.**

The following versions are **hard-pinned** in `package.json` (exact versions, no `^`) and `pnpm.overrides`. CI enforces them via `scripts/check-stack-pins.sh` — the build will fail if any drift. Active runtime drift is checked separately by `scripts/check-node-runtime.sh`; local release checks and CI must run on Node 24 + pnpm 10.23.0, not Node 25.

| Package | Pinned | Why this version |
|---|---|---|
| `next` | **15.5.18** | Next 16 forced Turbopack, renamed `middleware.ts` → `proxy.ts`, changed `revalidateTag` signature, and shipped CVE-2025-66478. Caused recurring dev-server crashes. Patch bump from 15.5.15 for the high-severity Next 15 advisory set reported by `pnpm audit` while staying on the stable 15.5 patch line. |
| `@next/bundle-analyzer` / `@next/eslint-plugin-next` | **15.5.18** | Next-adjacent tooling must stay on the same patch as `next`; CI enforces this to avoid silent analyzer/lint drift. |
| `react` / `react-dom` | **18.3.1** | React 19 nullable `RefObject<T \| null>` typing breaks third-party libs (Framer Motion 12). Wait until Next 17 makes React 19 the default. |
| `framer-motion` | **11.18.2** | v12 requires React 19. |
| `tailwindcss` / `@tailwindcss/postcss` | **4.2.2** | CSS-first config is working; don't risk a re-migration. |
| Bundler | **Webpack** (NOT Turbopack) | Turbopack still has gaps in our codebase (module factory race conditions, framer-motion chunk bugs). |
| Node | **24 LTS** | Active LTS on Vercel with support through April 2028. Runtime-only upgrade from Node 20 after Node 20 reached EOL; do not bundle this with Next/React/Turbopack changes. |
| `protobufjs` | **`^7.5.6`** | Transitive via `posthog-js > @opentelemetry/otlp-transformer`. Versions ≤7.5.5 carry 4 high-severity CVEs (code injection, prototype pollution, DoS). Override floor must stay at 7.5.6+; `pnpm audit --audit-level=high` in CI enforces this. |

### Rules for Codex / any AI assistant working on this repo

1. **NEVER** suggest or apply `pnpm add next@latest` (or react/tailwind/framer-motion). When you see an outdated version, that is **intentional**.
2. **NEVER** rename `middleware.ts` → `proxy.ts`. Next 15 uses `middleware.ts`. The rename is a Next 16-only change.
3. **NEVER** add a second arg to `revalidateTag()` (e.g. `revalidateTag("foo", "max")`). That's a Next 16-only API.
4. **NEVER** type refs as `RefObject<T | null>`. Use `RefObject<T>` — React 18's `useRef<T>(null)` already returns the right type.
5. **NEVER** add `--turbopack` or `--turbo` flags to dev/build scripts.
6. **NEVER** edit `scripts/check-stack-pins.sh` to relax pins unless the user explicitly tells you "we are upgrading X to Y".
7. **NEVER** bypass `--frozen-lockfile` or use `--no-frozen-lockfile` in CI.

### Process for an intentional upgrade (when the user approves one)

1. **One major upgrade per PR.** Never bundle Next + React + framer-motion in a single PR. Runtime, framework, and bundler upgrades are separate workstreams; a Node bump is not permission to bump Next, React, Tailwind, Framer Motion, or switch bundlers. (This is exactly how we got into the mess that took a week to undo.)
2. **6-month soak rule.** A new major version doesn't enter the stack until it's been GA for 6+ months **and** the framework above it (e.g. Next) ships it as the default.
3. Update **all of these in lockstep**:
   - `package.json` `dependencies` / `devDependencies`
   - `package.json` `pnpm.overrides`
   - `scripts/check-stack-pins.sh` `EXPECTED_*` constants
   - This Stack Pin Policy table
   - Add a Gotchas entry if the upgrade has subtle behavioral changes
4. **Verify the full intake flow E2E** before merging — typecheck/build alone is not enough.
5. Reference the prior context in `~/.Codex/projects/-Users-rey-Desktop-instantmed/memory/decisions.md`.

If you are an AI and the user asks you to upgrade something on this list, **stop and confirm** before touching the lockfile. The user has been bitten by this and wants explicit acknowledgment.

---

## Code Conventions

### Components & Styling
- `"use client"` at top for client components; server components default (no directive)
- `@/` path alias for all imports — always use it
- Tailwind utility classes + `cn()` from `lib/utils` for conditional classes
- shadcn/ui + Radix primitives for all form elements
- CVA (class-variance-authority) for component style variants
- lucide-react exclusively for icons
- Framer Motion via `motion` — always respect `useReducedMotion()`. Default spring (stiffness 100, damping 10) produces mechanical feel — run `/emil-design-eng` before shipping any animated component for a `| Before | After | Why |` spring-physics audit
- Public marketing/auth surfaces use abstract clinical or account marks, not fake people. DiceBear is allowed only for patient-selected account avatars or internal placeholder contexts.

### Form Inputs
- **Yes/no toggles**: shadcn `Switch` (NOT checkboxes)
- **Consent acknowledgments**: shadcn `Checkbox`
- **Form validation**: react-hook-form + Zod schema + `zodResolver`

### State & Data
- Zustand for intake flow state (persisted to localStorage, 24h expiry)
- React hooks for local component state
- `revalidatePath("/path")` after server mutations

### Server Actions
- Return shape: `{ success: boolean; error?: string; data?: T }`
- Auth: `requireRoleOrNull(["doctor", "admin"])` — returns user or null, non-throwing. Add `"support"` to the list for non-clinical ops actions.
- Cache busting: call `revalidateStaff({ intakeId?, patientId?, ops?, identity?, settings?, emails?, content?, scripts?, paths? })` from `lib/dashboard/revalidate-staff.ts` (and `revalidatePatient({ patientId?, intakeId?, settings?, documents?, followupId?, account? })` for patient-side mirrors) instead of scattering hardcoded `revalidatePath("/doctor/...")` strings. As of Phase 1.3 (2026-05-11), 132 of 139 hardcoded call sites in `app/actions/` use the central helpers; the remaining 7 are intentional (`revalidatePath("/")` after account closure, `revalidatePath("/request")` after service-catalog edits — public marketing surfaces, not staff).
- No `console.log` — ESLint errors on `no-console`. Use Sentry for logging

### Component Decision Tree
- **Form elements**: shadcn/ui from `@/components/ui/`
- **Simple display tables**: shadcn `Table` from `@/components/ui/table`
- **Loading states**: `SkeletonCard`, `SkeletonForm`, `SkeletonList`, `SkeletonDashboard` from `@/components/ui/skeleton`
- **Toasts**: `toast` from `sonner` (success, error, promise variants)
- **Error handling**: Route-level `error.tsx` files for staff pages; flow-specific `StepErrorBoundary` (`components/request/`); global connectivity banner via `NetworkStatus` from `@/components/ui/error-recovery`
- **Card surfaces**: Solid depth pattern — `bg-white dark:bg-card border border-border/50 shadow-md shadow-primary/[0.06]`. See DESIGN.md §5

## Design Tooling — Impeccable + Emil

Two skill packs are installed globally (`~/.Codex/skills/`) and supersede any generic design skills. Apply to all UI/UX/styling/animation/component-review work.

### Bootstrap requirement

**`/impeccable teach` must be run once per cold project context before any artifact-level Impeccable command.** It loads the full design-system snapshot so subsequent commands have correct token/component references. Skip it and commands fall back to generic defaults — wrong for this codebase.

### Do NOT reinstall overlapping packs

Do not install `frontend-design`, `design-system`, or similar skills. Impeccable supersedes them. Overlapping packs bloat the skill registry on every turn and cause redundant invocations.

### Impeccable — 17 commands

| Command | Lens — when to use |
|---------|-------------------|
| `/impeccable` | Full-page audit: runs shape → critique → audit → layout → typeset → colorize → polish in sequence. Comprehensive pass on a new or heavily changed page. |
| `/shape` | Plan UX and information architecture **before writing code**. Layout decisions, hierarchy, interaction model. Always run first on a new feature or page. |
| `/critique` | Evaluate a completed design from a UX perspective: usability, clarity, conversion friction, trust signals. |
| `/audit` | Technical quality checks: WCAG AA, semantic HTML, ARIA, color contrast, keyboard nav. Run before shipping any patient-facing surface. |
| `/typeset` | Fix font choices, hierarchy, size scale, line-height, letter-spacing, reading rhythm. Use when type feels off or dense. |
| `/colorize` | Add or correct strategic color. Use when a surface is too monochromatic or drifts from the Morning Canvas palette. |
| `/animate` | Add or improve motion: entrances, transitions, micro-interactions. Pairs with Framer Motion v11 + `useReducedMotion()`. |
| `/delight` | Add personality moments: micro-copy, unexpected positive feedback. Use sparingly — post-UX-solidification only. |
| `/bolder` | Amplify timid designs: increase contrast, weight, scale. Use when a design is too safe or forgettable. |
| `/quieter` | Tone down visually aggressive or overstimulating UI. Important for a health/trust context — anxious pages hurt conversion. |
| `/overdrive` | Push interfaces past conventional limits for high-impact hero or marketing moments. Top-of-funnel only. |
| `/layout` | Fix spacing, alignment, visual rhythm, grid issues. Use when layout feels loose, unbalanced, or inconsistent. |
| `/distill` | Strip to essence — remove clutter, redundancy, decoration. Use when a page has accumulated visual debt. |
| `/clarify` | Improve UX copy, error messages, microcopy, empty states, CTAs. Use when wording creates friction or sounds clinical. |
| `/adapt` | Adapt layouts across breakpoints. Use when a design breaks or degrades on mobile/tablet. |
| `/polish` | Final quality pass before shipping: alignment, pixel precision, dark-mode parity, focus rings, icon sizing. Run last. |
| `/optimize` | Diagnose rendering performance: layout thrashing, paint cost, animation frame drops. Use when a component feels slow. |

### Emil — motion / spring / component-feel

`/emil-design-eng` applies Emil Kowalski's philosophy on animation physics, spring curves, and interaction depth. Use it when:
- Motion feels mechanical or linear (wrong spring stiffness/damping)
- Components lack physical weight or responsiveness on hover/press
- You want a `| Before | After | Why |` audit of animation decisions

Emil is complementary to `/animate` — `/animate` adds motion; Emil makes existing motion feel physically correct.

### Decision rule

Any message touching UI / UX / styling / animation / component review: scan available-skills, pick the matching Impeccable command or Emil, and invoke it. Do not paraphrase around the skill.

---

## Pricing

All prices in `lib/constants/index.ts` (`PRICING`). Stripe IDs mapped in `lib/stripe/price-mapping.ts`.

**Stripe payment-state invariant:** Webhook and fallback payment confirmation must never mark an intake paid from a stale Checkout Session. Paid transitions are guarded by the current stored `intakes.payment_id`, retryable intake status, and unpaid/failed `payment_status`. Expiry and async failure handlers already use the same current-session guard.

**Refund policy (updated 2026-05-20):** All refundable categories (med cert, prescription, consult) get a **100% refund on decline**. The 50%-partial-on-consult rule was retired after operator feedback. The manual `issueRefundAction` can also top up a `partially_refunded` intake to full by refunding only the remaining `amount_cents - refund_amount_cents` against the original Stripe payment intent, and flips `payment_status` to `refunded` once the running total covers `amount_cents`. Support staff can issue refunds with a $100 per-refund cap and a 3-per-rolling-24h limit; admin and doctor are uncapped.

| Service | Price | Stripe Env Var |
|---------|-------|----------------|
| Med cert (1 day) | $24.95 | `STRIPE_PRICE_MEDCERT` |
| Med cert (2 day) | $29.95 | `STRIPE_PRICE_MEDCERT_2DAY` |
| Med cert (3 day) | $39.95 | `STRIPE_PRICE_MEDCERT_3DAY` |
| Repeat prescription | $29.95 | `STRIPE_PRICE_REPEAT_SCRIPT` |
| New prescription | $49.95 | Uses `STRIPE_PRICE_CONSULT` (routed via consult flow) |
| General consult | $49.95 | `STRIPE_PRICE_CONSULT` |
| ED consult | $49.95 | `STRIPE_PRICE_CONSULT_ED` |
| Hair loss | $49.95 | `STRIPE_PRICE_CONSULT_HAIR_LOSS` |
| Women's health (UTI + new/switch pill, live 2026-06-15) | $49.95 | `STRIPE_PRICE_CONSULT_WOMENS_HEALTH` |
| Weight loss (gated future subtype) | $89.95 reserved | `STRIPE_PRICE_CONSULT_WEIGHT_LOSS` |
| Priority fee (Express Review) | $9.95 | `STRIPE_PRICE_PRIORITY_FEE` |
| Referral letter | $29.95 | — (display only, not yet Stripe-mapped) |
| Pathology request | $29.95 | — (display only, not yet Stripe-mapped) |

## Key Workflows

**Intake flow:** Step-based wizard at `/request?service=<type>`. Managed by `lib/request/step-registry.ts`. Steps are React components in `components/request/steps/`. See docs/ARCHITECTURE.md for full step sequences.

**Staff dashboard:** Admin, doctor, and support work are intentionally unified into one staff cockpit at `/dashboard`. Phase 2 of the remaster (2026-05-12) made this the real surface: role-aware content (admin gets owner setup/readiness cards above the same clinical queue; doctor gets the queue; support is redirected to the bounded support ops cockpit at `/admin/ops`), the `SystemHealthPill` in the header (90s poll on `/api/admin/system-health`; `POLL_INTERVAL_MS` in the component is the source of truth), and the doctor availability toggle. Legacy `/admin`, `/doctor`, `/doctor/dashboard`, and `/doctor/queue` URLs redirect here from `next.config.mjs`. Both `/admin/*` and `/doctor/*` route trees still render through the same `OperatorShell` (Phase 1.2) with role-aware nav via `getStaffNav(profile)` — admins get the full operator nav, doctors get the clinical subset, support staff get one `/admin/ops` entry with nested recovery links inside the ops page. The retired `DashboardSidebar variant="doctor"` was removed; `DoctorShell` (panel provider + intake notification listener + bottom-tab `DoctorMobileNav`) still wraps doctor pages inside the shared `OperatorShell` so clinical-specific UX (mobile bottom tabs, real-time notifications, the keyboard shortcut hint) is preserved. Staff pages should use `components/operator/*`, role-aware navigation from `lib/dashboard/staff-navigation.ts` (`getStaffNav(profile)`), compact case summaries from `lib/doctor/case-summary.ts`, and the central `revalidateStaff()` helper from `lib/dashboard/revalidate-staff.ts` for cache busting. Use `STAFF_*_HREF` constants from `lib/dashboard/routes.ts` for shared staff navigation; keep `ADMIN_*_HREF` only for admin pages that still own a real operational surface. Do not reintroduce dashboard/doctor aliases for paths that already resolve to `/dashboard`. Keep dashboards bounded and scannable: primary desktop staff pages should avoid whole-page scrolling by using split panes, short action lists, tabs, and detail drawers/panels. If a staff page has more controls than fit in one viewport, keep the page itself fixed-height and put secondary detail inside internal panes.

**Health guide workflow:** Public article guides live in `content/blog/*.mdx` and render through `components/blog/article-template.tsx`. Guide bodies are education-only: no mid-article consultation CTA, no service CTA card, no related-service acquisition panel, and no location SEO cross-link block. Use `pnpm content:audit` before shipping guide/content changes. Every rewritten guide needs at least two GPT-generated local visuals, ideally three for high-intent or clinical topics. Article visuals are registered in `lib/blog/visuals.ts`, rendered by `components/blog/article-visuals.tsx`, and generated as local WebP assets under `public/images/blog/<slug>/` via `pnpm blog:generate-visual-images -- --renderer=gpt-image-2`. The generation script adds a deterministic `InstantMed` wordmark after image generation; do not ask the image model to draw the brand mark itself. Controlled short copy may appear inside the GPT-generated visual only when it comes from the registry; the same labels and clinical distinctions must also render in React/HTML for accessibility, review, and indexing. **Visual quality bar:** every generated article image must be an educational asset, not decoration. It must teach through labelled panels, anatomical callouts, comparison matrices, warning hierarchies, step pathways, body maps, lab explainers, or other concrete instructional structure. Reject and regenerate any output that is mostly a blank phone, blank document, blank certificate, medicine box, inhaler still life, balance scale, shield/pill card, abstract icon row, beige tabletop, generic desk flat lay, decorative hero image, or any low-information metaphor that could be reused on another article. Baseline rewritten guides should answer the practical question in the first screen, have at least six H2 sections unless genuinely narrow, target 1,200+ words, include a visible sources/references section, and show red flags or care boundaries when clinically relevant.

**Specialty services (ED, Hair Loss):** Dedicated landing pages at `/erectile-dysfunction` and `/hair-loss` are the top-level marketing surfaces for these pathways (alongside `/medical-certificate` and `/prescriptions`). Both CTAs route into `/request?service=consult&subtype=ed` and `/request?service=consult&subtype=hair_loss` respectively. They share the `consult` service type but use subtype-specific step sequences in `CONSULT_SUBTYPE_STEPS` (see `lib/request/step-registry.ts`). ED and hair-loss collect medical history inside their subtype health screens, so they do not add the generic `medical-history` step again. Current positioning is **form-first doctor review**: patient completes the structured form, the doctor reviews, and the doctor calls/messages only if clinically needed. Do not hard-promise "no call needed" for prescribing pathways. Service hub (`/request`) shows 5 active cards (med-cert, repeat prescriptions, ED, hair loss, women's health — live 2026-06-15) + 1 coming-soon (weight management); the services dropdown and footer match. **General Consult was retired publicly on 2026-05-20** because it served as a back-channel for gated services (weight loss, women's health) with no structured screener; Google Ads never targeted it (0 paid clicks in 180 days), so removal carried no acquisition cost. `/consult` now renders a services-overview page (preserves the SEO surface for "online doctor" queries) and `/general-consult` 301s into it. The `consult` service type stays in code as the parent category for ED and hair-loss subtypes; the `'general'` consult subtype was removed from `ConsultSubtype`. Declined consults (including ED + hair-loss) receive a full refund on decline as of 2026-05-20 — see the Refund Policy note above and `REFUND_ON_DECLINE_CATEGORIES` in `app/actions/decline-refund.ts`. **ED intake uses 4-step flow:** ed-goals (goal + duration) → ed-assessment (visual IIEF-5, validated 5-question instrument producing `iiefTotal` 5–25) → ed-health (ONE always-mounted screen consolidating safety screening + medical history — nitrate hard block, cardiac soft blocks with GP clearance; flattened 2026-07-02 per the #209 no-progressive-reveal rule, hair-loss-health same — answered sections stay editable, do NOT reintroduce phase unmounting or "Safety N of M" eyebrows) → ed-preferences (lifestyle framing: daily/as-needed/doctor-decides, **no Schedule 4 drug names** per TGA). Common tail adds height/weight/BMI for ED subtype. IIEF-5 score persisted for followup delta tracking. **Follow-up tracker:** Follow-up infrastructure exists, but subscriptions/monthly prescribing and staff-heavy follow-up are not part of the current one-off business model unless `docs/BUSINESS_PLAN.md` is updated.

**Prescription workflow:** Patient submits → Doctor reviews in portal → Doctor approves for prescribing → Parchment iframe opens in slide-over panel → Doctor writes eScript inside InstantMed → Parchment webhook (`prescription.created`) auto-marks script sent with SCID → Patient notified via email. Approval into `awaiting_script` and Parchment launch are gated on complete prescribing identity (DOB, sex, Medicare details, phone, structured address) using the same patient snapshot rules that power `/admin/ops/prescribing-identity`; incomplete cases must be fixed before prescribing. Feature flag: `parchment_embedded_prescribing` (DB toggle). Fallback: "Mark Sent Manually" button for external prescribing. Patients enter one medication per repeat request as plain free text in the intake (`components/request/steps/medication-step.tsx`) — the PBS reference-search combobox + `/api/medications/search` + `lib/clinical/pbs-client.ts` were retired 2026-06-28 (#211). The typed name is doctor context only: do not present it as MIMS, prescribing advice, or Parchment preselection; the doctor still selects the medicine inside Parchment/MIMS. Free-text backstops are unchanged (`isControlledSubstance` hard block, `detectDedicatedServiceForMedication` steer, the `dedicated_service_medication` attention flag). Parchment confirmed custom-domain iframe whitelist for `https://instantmed.com.au` and `https://www.instantmed.com.au` on 2026-05-01; `lib/parchment/embed-policy.ts` defaults must continue to allow those hosts plus local/Vercel preview hosts. Key files: `lib/parchment/client.ts`, `lib/parchment/sync-patient.ts`, `lib/parchment/embed-policy.ts`, `components/doctor/parchment-prescribe-panel.tsx`, `app/api/webhooks/parchment/route.ts`, `app/actions/parchment.ts`.

**Operational controls:** Admin control surface lives at `/admin/features`; DB-backed mutation path is `app/actions/admin-config.ts` → `lib/feature-flags.ts`. `/admin/features` is a bounded operator console, not a long settings page: platform/service kill switches stay visible in the critical strip, and lower-priority controls sit inside compact tabs with internal scrolling only. Runtime enforcement lives under `lib/operational-controls/`: capacity limits fail closed when enabled but the count RPC fails, and medication blocklist extraction is shared across authenticated and guest checkout. Business hours are review-timing reference only, not a checkout blocker. `/admin/ops` is the support cockpit (admin and support share the same view): five recovery counter cards (Payment failures, Webhook DLQ, Parchment unsynced, Missing identity, Google Ads conversions), an "Integrity (weekly invariants)" strip (Review SLA backlog, Cert + refund orphans, Refund record anomalies) fed by `getOperationalInvariants()` in `lib/admin/ops-invariants.ts`, and a "Recent (last 24h)" recoveries list. The "Google Ads conversions" card counts failed server-side conversion uploads in the last 7d via `getGoogleAdsConversionUploadHealth()` (DB-only over `audit_logs` action `google_ads_conversion_upload`; deliberately NO live Google Ads preflight API call on this hot page — the full preflight/health panel stays on `/admin/analytics`), and deep-links to `/admin/analytics`. Env/config signals (Stripe price config, Telegram alerts, auth-email health) belong on `/admin/features`, not `/admin/ops`.

**Checkout safety enforcement:** Authenticated, guest, and retry-payment checkout paths must call `validateSafetyFieldsPresent()` before `checkSafetyForServer()`. Missing safety-critical answers return `REQUEST_MORE_INFO` and are written to `safety_audit_log` via the service role with sanitized metadata only. Allowed checkout outcomes update intake triage fields (`risk_tier`, `triage_result`, `triage_reasons`, `requires_live_consult`, `live_consult_reason`) after `intake_answers` has been saved.

**Repeat Rx subscription:** Dormant/future strategy. Patient-facing checkout, nudge cron, email template, env requirement, and display price were retired; the current business model is one-off transactions only. Historical subscription webhook/account support may remain for compatibility. Do not market, default, or expand subscriptions until `docs/BUSINESS_PLAN.md` and `docs/REVENUE_MODEL.md` are explicitly updated.

**Priority fee (Express Review):** $9.95 add-on toggle on checkout. Adds second line item to Stripe session. Sets `is_priority` on intake → doctor queue sorts priority-first.

**Certificate pipeline:** Doctor approves → PDF generated → Uploaded to private Supabase Storage → Patient emailed dashboard link (not attachment). See docs/ARCHITECTURE.md for 9-step generation flow.

**Phone number:** Required for prescriptions + consults + specialized pathways. NOT for med certs.

**Safety/payment consent:** Not a standalone step. Every flow collects final consent in the unified review+pay step (`review-step`); med-cert's step id stays `checkout` for analytics continuity — the standalone checkout-step was retired 2026-06-28.

**Guest checkout:** Creates profile without `auth_user_id` → Stripe checkout → redirects to `/auth/complete-account` for account linking. After clinical answers are persisted, Stripe setup failures must keep an operator-visible `checkout_failed` intake instead of deleting the record.

**Referrals:** $5 credit to both parties. Patient shares `?ref=` link. UI: `components/patient/referral-card.tsx`.

---

## Gotchas

- **Build needs 8GB heap**: `NODE_OPTIONS='--max-old-space-size=8192'` set in build/typecheck scripts
- **Vercel build cost is Build CPU Minutes (frequency × duration), not runtime** (see docs/OPERATIONS.md §Build & Deployment Cost): two config-only levers, each a one-line revert. (1) `vercel.json` `"ignoreCommand": "bash scripts/vercel-ignore-build.sh"` skips the per-push PREVIEW build on every non-`main` branch (previews were ~half of all builds), AND skips the `main` prod build when a commit touches only non-runtime paths (`*.md`, `docs/`, `e2e/`, `**/__tests__/`, `*.test.*`/`*.spec.*`, `.github/`) — docs/test/CI-only commits were ~26% of `main` builds (73/284 in 30d) and change nothing the server runs (the edit ships with the next runtime deploy). Runtime paths (`app`/`components`/`lib`-non-test/`public`/`scripts`/`supabase`/`package.json`/lockfile) still build, and the gate fails safe (builds) when the diff can't be computed (shallow clone / root commit). Safe because the required CI `build` check (`ci.yml`: lint+typecheck+unit+Playwright E2E+Lighthouse, all vs **localhost**) is the real PR gate and does NOT depend on a Vercel preview; branch protection on `main` requires only `build`, NOT a Vercel deployment status. Overrides (latest commit message): `[deploy]` forces a `main` build; `[preview]` forces a branch preview (re-fires the non-required `e2e-preview.yml` smoke against the live URL). (2) `next.config.mjs` `typescript.ignoreBuildErrors: true` + `eslint.ignoreDuringBuilds: true` stop `next build` re-running lint/type-check — measured 48.9s of a 186s prod build, redundant because CI (`pnpm lint`+`pnpm typecheck`) and `pnpm ci` already gate every merge. Caveat: a direct-to-`main` push that bypasses CI (`enforce_admins:false`) loses the in-build type/lint net — keep shipping via PR. Build machine tier is **Enhanced** (≈2× CPU-minutes/build); downgrade to Standard in the Vercel dashboard for cost (dashboard-only, not `vercel.json`). Do NOT switch to Turbopack to speed builds — hard-pinned off (Stack Pin Policy).
- **Stack pins are enforced in CI**: See the Stack Pin Policy section above. `scripts/check-stack-pins.sh` runs in `ci.yml` and fails the build if Next/React/Tailwind/Framer Motion drift. Read the policy before any dep change.
- **Middleware filename**: Next 15 uses `middleware.ts` at repo root (NOT `proxy.ts` — that was Next 16's rename). Don't accept LLM/AI suggestions to rename it
- **`revalidateTag` signature**: Next 15 takes a single tag arg — `revalidateTag("foo")`. Do NOT add a second `"max"` cache profile arg; that's a Next 16-only API
- **`RefObject` typing**: With React 18 use `RefObject<HTMLDivElement>`, NOT `RefObject<HTMLDivElement | null>`. The `| null` is React 19 syntax — `useRef<T>(null)` returns `RefObject<T>` whose `.current` is already `T | null`
- **Controlled substances — ONE term list (unified 2026-07-03)**: `lib/clinical/controlled-substances.ts` (`CONTROLLED_SUBSTANCE_TERMS`) is the single source of truth. `isControlledSubstance(name)` in `lib/clinical/intake-validation.ts` derives its regex patterns from it (UI passes the flag to `validateIntake()` which blocks progression — no override), and the repeat-script server blocklist `BLOCKED_S8_TERMS` (`lib/validation/repeat-script-schema.ts`, fuzzy typo matching) re-exports the same list. Pre-unification the two copies had diverged (intake blocked tramadol/cannabis/testosterone + AU benzo brands the server waved through; the server knew ketamine + codeine compounds the intake didn't) — `lib/__tests__/controlled-substances-parity.test.ts` pins both layers to the shared list, so add terms THERE only (adding widens every layer at once — the safe direction; removals are clinical policy, check docs/CLINICAL.md). Bare "codeine" is deliberately unblocked (combination-product repeats go to the doctor, not an intake wall). The orphaned `/api/terminology/amt/search` route (dead since the #211 medication-search retirement) held a third stale copy and was deleted.
- **Dedicated-service medication routing (repeat-Rx)**: `detectDedicatedServiceForMedication()` in `lib/clinical/medication-service-routing.ts` flags finasteride/minoxidil → Hair Loss and OCP brands/ingredients → Women's Health when entered into the generic repeat/prescription medication step. It is an intent-aware **soft block** (NOT a hard redirect): the medication step shows a steer with a primary "Continue in {service}" CTA (deep-links `/request?service=consult&subtype=…`, preserving attribution) and an explicit "keep as repeat" escape — because finasteride **5 mg / Proscar / Avodart / Duodart are legitimate BPH repeats** (excluded via `BPH_MARKERS`) and continuing the same pill is deliberately a cheap repeat (see the `ocp_repeat → repeat-script` rule in `lib/request/consult-subtypes.ts`). UTI antibiotics are intentionally **out of scope** (acute courses aren't repeats). Server backstop (never client-only): `deriveIntakeFlags` emits the `dedicated_service_medication` attention flag (`lib/clinical/intake-flags.ts` + `derive-intake-flags.ts`) so the reviewing doctor always sees it. Guarded by `lib/__tests__/medication-service-routing.test.ts`.
- **Repeat quantity & supply standard**: default **original script + 2 repeats** (≈3 months for a daily medicine) across repeat-Rx, hair loss, and ED — a DEFAULT the doctor applies in Parchment, NOT a code-enforced field and NOT a fixed promise (doctor discretion is explicit; AHPRA/TGA require individualised prescribing). Single source of truth: `lib/clinical/repeats-policy.ts` (`DEFAULT_REPEATS`, `STANDARD_SUPPLY_MONTHS`, `REFILL_REMINDER_WINDOW_MIN/MAX_DAYS`) — shared by the intake "what to expect" note (`getRepeatsExpectation`, rendered on the review step for repeat-Rx/hair-loss/ED only; null for med-cert/women's-health/weight-loss; ED omits the months claim since on-demand), the reactivation reminder (`/api/cron/refill-reminders`, fires ~week 10-11 before the supply runs out), and docs/CLINICAL.md §Repeat Quantity & Supply Standard. Patient-facing copy is expectation-setting only ("if approved … usually"), never a guarantee/inducement or subscription implication. Guarded by `lib/__tests__/repeats-policy.test.ts` + a review-step assertion in `e2e/prescription-flow.spec.ts`.
- **PHI encryption**: AES-256-GCM field-level. Controlled by `PHI_ENCRYPTION_*` env vars. See docs/SECURITY.md
- **Rate limiting fallback**: Two systems — Redis (general API): fails open when unavailable. Doctor actions (DB-backed): falls back to in-memory `Map` with half limits
- **Certificate IDs**: `crypto.randomInt()` not `Math.random()` — security requirement
- **Name validation**: Unicode-aware `/^[\p{L}\s'-]+$/u` for international names
- **AHPRA validation**: Format check `/^[A-Z]{3}\d{10}$/` only — not a real AHPRA lookup
- **No template editor**: Certificates use static PDF templates in `/public/templates/` with `pdf-lib` text overlay — not a WYSIWYG editor
- **GP comparisons**: Kept but subtle — `text-xs text-muted-foreground`, no crossed-out prices
- **Copy-only changes need `/clarify`**: Any PR that's primarily copy (regulatory sweeps, microcopy updates, FAQ edits, CTA text) must have `/clarify` run before committing. Clinical and legal language that passes compliance review routinely creates UX friction. The pass takes 2 minutes; skipping it ships friction into a high-anxiety patient flow
- **Public reviews/social proof**: Keep the Google stars badge stars-only. Do not render review counts, numeric star ratings, testimonials, or aggregate-rating schema on public regulated-health advertising surfaces.
- **Doctor model**: System supports multiple doctors. Don't advertise doctor count, individual doctor names, FRACGP, peer review, or team size claims unless verified and approved.
- **Dev routes blocked in prod**: Middleware blocks `/api/test/*`, `/email-preview*`, `/sentry-test*`, `/cert-preview*` in production/preview (exception: `PLAYWRIGHT=1`). `/cert-preview` is a retired dev route and stays fail-closed in middleware. Note: `(dev)` route group files resolve to their path WITHOUT the group prefix — every new `(dev)` page/route must have a corresponding middleware block added manually.
- **Supabase migrations**: Squashed to 1 baseline, then layered fixes on top. Current count on disk: **94 migration files** (1 baseline + 93 incremental). Most recent: `20260711193000_reopen_approved_intake_after_cert_revocation.sql` (widens the guarded `approved → in_review` reversal in the `validate_intake_status_transition` trigger to require only a **revoked certificate** — the true safety invariant — instead of the batch-review receipt (`ai_approved` + `batch_reviewed_at/by`). Fixes the 30s approval-undo path stranding a paid patient with a revoked cert + a stuck `approved` intake: undo applies to MANUAL approvals (`ai_approved=false`) and never stamps a batch receipt, so the old guard rejected its reopen and swallowed the error. Ordinary callers still cannot reverse `approved` (no revoked cert). App-layer `VALID_STATUS_TRANSITIONS.approved` intentionally stays strict — the reversal is DB-trigger-only via direct service-role updates in `revoke-ai-approval.ts`/`undo-cert-approval.ts`. APPLIED to prod + verified 2026-07-11; guarded by `lib/__tests__/batch-review-state-transition.test.ts`). Immediately before it, `20260711071506_allow_guarded_ai_med_cert_reopen.sql` (#318 — first cracked one-way `approved` to let a doctor reopen an AI-approved med cert during batch review, guarded on the batch receipt; superseded by the looser revoked-cert guard above. APPLIED to prod 2026-07-11). Immediately before those, `20260710174000_idempotent_certificate_resends.sql` (adds service-role-only transactional reservation/finalisation for certificate resend attempts, per-user rolling-window enforcement, and dispatcher reconciliation; APPLIED to prod + verified 2026-07-10). Immediately before it, `20260710173000_atomic_certificate_corrections.sql` (serializes certificate correction requests and reissues, binds the exact replacement PDF version, and prevents concurrent pending replacements; APPLIED to prod + verified 2026-07-10). Immediately before it, `20260710170000_close_patient_accounts_atomically.sql` (atomically tombstones closed patient profiles, minimizes retained identifiers, blocks stale authenticated sessions from patient PHI, and serializes closure against new intake creation; APPLIED to prod + verified 2026-07-10). Prior to that, `20260706081426_add_email_suppressions.sql` (account-less marketing suppression list — the email-keyed unsubscribe target for recipients with no profile, e.g. partial-intake draft recovery; Spam Act s18 fix from the 2026-07-06 email audit. Service-role only, RLS on, zero anon/authenticated grants. APPLIED to prod + verified 2026-07-06. Written by `/api/unsubscribe` for `e_`-prefixed tokens from `signEmailUnsubscribeToken`; read fail-CLOSED by `getSuppressedEmails` in `lib/email/suppression.ts` before partial-intake recovery sends). Prior to that, `20260625103000_merge_patient_profiles_moves_prescriptions.sql` (patient-profile merge also moves `prescriptions` rows to the surviving profile so merged patients keep their prescription history; shipped with the prescribing rebuild #189–#197, applied to prod 2026-06-25). Prior to that, `20260624100834_add_reactivation_email_sent_at_to_intakes.sql` (adds nullable `reactivation_email_sent_at timestamptz` + a partial index to `intakes` for the med-cert reactivation email cron at `/api/cron/cert-reactivation`. Additive/safe; APPLIED to prod + verified 2026-06-24. The cron ships OFF behind `CERT_REACTIVATION_EMAILS_ENABLED` and is marketing-consent gated — one "need another certificate?" nudge per past med-cert patient whose most recent certificate is 35-120d old; `intakes.reactivation_email_sent_at` dedups; NOT a subscription, creates no order). Prior to that, `20260623094758_rename_consult_service_label.sql` (renames the shared `consult` service row "General Consult" → "Consult" — that row is the active parent for the live ED/hair-loss/women's-health consult subtypes, so the publicly-retired generic-service name was misleading admin + patient tracking and made the 2026-06-23 audit read it as a retired-service leak. Label-only, no claim/availability change; APPLIED to prod + verified 2026-06-23). Prior to that, `20260623090952_revoke_compliance_audit_summary_authenticated.sql` (completes the 2026-06-12 lockdown — that migration revoked `compliance_audit_summary` from `anon` only and deliberately left the `authenticated` SELECT, banking on the staff-only `compliance_audit_log` RLS; this revokes `authenticated` too so the surface matches `v_stuck_intakes`/`document_verifications`. Non-breaking — `security_invoker` + RLS already returned zero rows to non-staff, verified via `has_table_privilege`/`information_schema.role_table_grants`. APPLIED to prod + verified 2026-06-23; `anon-readable-surfaces-contract.test.ts` now asserts every PHI surface is revoked from BOTH anon AND authenticated so it can't silently regress). Prior to that, `20260612000000_lockdown_anon_readable_surfaces.sql` (P0 PHI-leak fix from the 2026-06-10 audit — revokes anon/authenticated SELECT on `v_stuck_intakes`, `document_verifications`, and `compliance_audit_summary`, sets `security_invoker=on`, and scopes the `profiles` INSERT policy to service_role. APPLIED to prod + verified, PR #123; guarded by `lib/__tests__/anon-readable-surfaces-contract.test.ts`). Prior to that, `20260611000000_add_heard_about_us_to_intakes.sql` (adds nullable `heard_about_us text` to `intakes` for the self-reported "How did you hear about us?" attribution survey — the only way to attribute referrer-stripped dark traffic, ~50% of orders. APPLIED to prod, verified live 2026-06-11. Acquisition metadata only — enum token `ai|search|friend|forum|ad|other`, NOT PHI, write-once. See the self-reported attribution Gotcha below). Prior to that, `20260610000000_add_refill_reminder_sent_at.sql` (adds nullable `refill_reminder_sent_at timestamptz` + a partial index to `prescriptions` for the one-off refill-reminder reactivation cron at `/api/cron/refill-reminders`. Additive/safe; apply with `supabase db push`. The cron ships OFF behind `REFILL_REMINDER_EMAILS_ENABLED` and is marketing-consent gated — it is the one-off reorder nudge, NOT a resurrection of the retired subscription-nudge model). Prior to that, `20260609000000_allow_checkout_failed_to_paid.sql` (adds `paid` to the allowed transitions from `checkout_failed` in the `validate_intake_status_transition` DB trigger; mirrors the same fix in `lib/data/intake-lifecycle.ts`. Real incident: a customer paid $29.95 for a work med cert on 2026-05-29, the Stripe session completed on a payment retry after an earlier checkout_failed session, but the webhook rejected the `checkout_failed→paid` transition and the intake sat stuck for 10 days. Intake manually recovered.) Prior to that, `20260607000000_atomic_approve_clears_claim.sql` (re-creates `atomic_approve_certificate` so STEP 5 also sets `claimed_by = NULL, claimed_at = NULL` — an approved intake is terminal and must not retain a review claim; backfills ~93 residual doctor claims on already-approved/declined intakes + ~41 phantom `System (Auto-Approve)` locks left by the pre-fix pipeline. Pairs with the code-side fix in `lib/clinical/auto-approval-state.ts` that sets `claimed_at` on claim and clears the claim on every relinquish). Prior to that, `20260602070000_restore_intake_abandonment_tracking.sql`, `20260528003557_add_profile_ihi_number.sql`, and `20260526100000_email_outbox_scheduled_for.sql` (adds `scheduled_for timestamptz` + partial index to `email_outbox` so the dispatcher can defer sending; powers the 30s cert approval undo window). Before those, `20260525000000_forbid_general_consult_inserts.sql` and `20260524090000_allow_forward_dated_med_certs.sql` (widens the AEST-aware cert `start_date` constraint to allow forward-dating up to 14 days; paired with PR #57's UI chip widening). Account closure state lives in `20260502003000_add_account_closure_to_profiles.sql`; archived audit rows are locked by `20260504193100_enable_rls_on_audit_logs_archive.sql`. 182 pre-squash files were removed on 2026-04-21 after a drift audit found they were never in the tracker and would break fresh clones via `supabase db push`. Full historical audit at `docs/audits/2026-04-21-migration-drift-audit.md`; current migration/conversion audit at `docs/audits/2026-05-03-supabase-conversion-audit.md`. Use `supabase db push` and `pnpm db:check-migrations` after linked migration changes. Note: the tracker still has a duplicate-name row for `add_delay_notification_sent_at` (both `20260403000001` and `20260403062235`); cosmetic only, clean up with `supabase migration repair` when convenient.
- **Auto-approval deterministic routing + claim hygiene (2026-06-07 incident fix)**: A med-cert auto-approval refusal whose verdict cannot change on retry MUST short-circuit straight to `needs_doctor` — never loop in the retry queue. The deterministic set lives in `DETERMINISTIC_FAILURE_PREFIXES` (`lib/clinical/auto-approval-state.ts`) and is pinned **both directions** (every deterministic flag the engine emits is covered; no dead prefixes) by `lib/__tests__/auto-approval-deterministic-routing-contract.test.ts`. The incident: `high_stakes_use_case:` (exam deferral, forklift/driving fitness) — plus `red_flags:`, DOB, and duration flags — were missing, so those structurally-un-approvable refusals were misclassified as **transient**, burning all 10 `auto_approval_attempts` over the retry cron's 8h `paid_at` window before finally reaching a doctor. Four med certs sat 9–46h while the cockpit showed "System (Auto-Approve) is reviewing", so it looked hung. **Do NOT add genuinely time/state-dependent reasons to the list** — `repeat_request_within_7d:` (the 7-day window slides), `missing_clinical_note_draft` / `draft_not_ready:` (a draft can be generated on a later pass), and the pipeline-level reasons (`no_doctor_available`, `pipeline_error`, etc.) must stay transient so they keep retrying. **Claim hygiene (same fix):** `claimForProcessing` now sets `claimed_at` alongside `claimed_by = SYSTEM_AUTO_APPROVE_ID` (without it, `release_stale_intake_claims` — which filters `claimed_at IS NOT NULL` — can never recover a crashed attempt, and `claim_intake_for_review`'s 30-min takeover math is NULL, so the doctor cockpit renders a phantom "Auto-approval check is running" lock with a broken `( minutes remaining)` via `formatClaimWarning`). `markNeedsDoctor` / `markFailedRetrying` / `recoverStale` and the dry-run rollback all now clear `claimed_by`/`claimed_at` on relinquish; terminal approval clears them inside the `atomic_approve_certificate` RPC (single source of truth for the manual + auto paths). Never set `claimed_by` without `claimed_at`.
- **Seeded E2E data is hidden from live ops reads**: `lib/data/seeded-e2e-data.ts` filters the seeded `E2E Test Patient` profile out of doctor queue, queue health, and operational queue stats unless `PLAYWRIGHT=1`, `E2E=true`, `E2E_MODE=true`, or `NODE_ENV=test`. Do not remove this boundary; failed E2E teardowns must not inflate production workload.
- **Duplicate-profile safety flag (2026-07-06)**: Every patient-scoped guard (repeat-within-7d, prior-approval trust, renewal detection) keys on `patient_id`, so a patient re-entering under a NEW email gets a fresh profile that reads as a clean first-time patient — the 2026-07-06 loophole (same person, two emails `adam.west@hotmail…` + `nicknamestaken@gmail…`, same DOB; the second could have silently auto-approved a 2nd cert). `findDuplicatePatientProfile` (`lib/clinical/duplicate-patient-detection.ts`) matches a DIFFERENT profile on normalized `full_name` + exact `date_of_birth` (both plaintext on `profiles`; no decryption). Wired into `lib/clinical/auto-approval-pipeline.ts` step 6d: a hit appends the `duplicate_patient_name_dob` attention flag to `attentionFlagCodes` → `evaluateAutoApprovalEligibility` emits `intake_attention_flags:` (a pinned `DETERMINISTIC_FAILURE_PREFIXES` entry) → med cert routes to `needs_doctor` immediately (NOT the transient retry queue — a duplicate doesn't stop being one on retry). The flag is also persisted to `intakes.risk_flags` so the doctor sees the calm `IntakeFlagsBadge`/`IntakeFlagsPanel` chip ("Possible duplicate profile (same name + DOB)") with the matched profile id in the tooltip. Attention signal only: it never declines/approves — the doctor decides. Fail-soft (lookup error → no duplicate, never blocks the pipeline). Currently med-cert-only (the only auto-approved path — prescriptions/consults already get a human); a render-time chip for those services is a possible follow-up. Guarded by `lib/__tests__/duplicate-patient-detection.test.ts`.
- **Med certs do not expire**: The `expire-certificates` cron was deleted on 2026-04-28 (commit `9c56ac612`). 54 silently-expired cert rows were backfilled to `valid`. Verification only treats `revoked` as inauthentic; `superseded` returns a "request the current version" message; everything else verifies. Don't reintroduce expiry logic — the DB trigger above will reject it.
- **No hand-written release logs**: `docs/RELEASE_LOG.md` was deleted 2026-05-23. Release evidence belongs in commit messages, GitHub Releases, and Sentry release tagging, not a markdown file that goes stale after the first deploy. Do not resurrect it as `docs/RELEASE_LOG_2026_Q3.md` or any variant. If the operator wants a live release feed, add a `system_release` audit row from the post-deploy smoke workflow and surface it on `/admin/ops` next to the recovery rows.
- **Intake state machine lives in TWO places — keep them in sync**: `lib/data/intake-lifecycle.ts` (`VALID_STATUS_TRANSITIONS`) is the app-layer enforcement; `validate_intake_status_transition()` is the DB-trigger enforcement. Both must be updated in the same commit whenever a transition is added or removed, otherwise one layer will silently reject what the other permits. The 2026-06-09 incident fixed `checkout_failed→paid` in the app but not the trigger, causing a DB error on every real recovery attempt. Migration `20260609000000_allow_checkout_failed_to_paid.sql` + `lib/__tests__/intake-lifecycle.test.ts` are the reference; the test file pins all `checkout_failed` transitions so the regression surface is covered. **One deliberate exception (2026-07-11):** the DB trigger permits a guarded `approved → in_review` reversal (only when the issued certificate is revoked) that the app map `VALID_STATUS_TRANSITIONS.approved` intentionally does NOT list. This is safe app-stricter-than-DB, not drift: the two legitimate reversals (batch-review revoke, 30s approval undo) do direct service-role updates and bypass the app map, and a pure allowed-list can't express the revoked-cert guard, so listing `in_review` would let an unguarded reopen pass app validation the DB then rejects (the wrong app⊋DB direction the parity contract forbids). Do NOT "sync" it by adding `in_review` to the app map — the `intake-state-machine-parity-contract` parser reads the ELSIF branch and would false-fail. See migration `20260711193000` + the note on the `approved` entry in `intake-lifecycle.ts`.
- **`payment-smoke.spec.ts` and `stripe-webhook.spec.ts` must never run against production**: Both tests construct fake Stripe events signed with the real `STRIPE_WEBHOOK_SECRET` and POST them directly to the webhook endpoint. Running either against `instantmed.com.au` writes fake events to the production database — `payment-smoke.spec.ts` floods the DLQ (141 fake entries before the 2026-06-09 fix); `stripe-webhook.spec.ts` writes fake DLQ entries and exercises payment/refund state transitions on real intakes. Both tests now throw at module load if `PLAYWRIGHT_BASE_URL` contains `instantmed.com.au`. The webhook also rejects `livemode: false` events in production as a second layer of defence. Never set `PLAYWRIGHT_BASE_URL=https://instantmed.com.au` in CI or local runs. Any new E2E test that POSTs fake signed Stripe events must add the same production guard.
- **Stripe webhook handlers must match dispatch Map (parity contract)**: `lib/__tests__/stripe-webhook-handler-parity-contract.test.ts` asserts every `.ts` file in `app/api/stripe/webhook/handlers/` (other than framework files `index.ts`/`types.ts`/`utils.ts`) is imported by `index.ts` and vice versa. The 2026-05-23 cleanup found 4 zombie handler files (`customer-subscription-deleted.ts`, `customer-subscription-updated.ts`, `invoice-payment-failed.ts`, `invoice-payment-succeeded.ts`) that survived on disk for weeks without being wired into the dispatch Map — they silently no-op'd those events. The contract test now blocks that bug class. Adding a new handler? Both create the file AND add the `["event.type", handlerFn]` entry to the Map in the same commit. Per OPERATIONS.md, `invoice.*` and `customer.subscription.*` events are deliberately retired in the one-off model — do not reintroduce them without an explicit business-model decision update in `docs/BUSINESS_PLAN.md`.
- **`@ai-sdk/anthropic` calls must pin `baseURL` explicitly**: the SDK reads `process.env.ANTHROPIC_BASE_URL` and appends `/messages` to it. Claude Code's interactive shell exports `ANTHROPIC_BASE_URL=https://api.anthropic.com` (without `/v1`), which makes the SDK call `/messages` instead of `/v1/messages` and return **404 "Not Found" on every model**. This cost hours of debugging when rotating Anthropic keys — three keys looked broken until we caught the env leak. Mitigation: `lib/ai/provider.ts`, `scripts/video-review/synthesize.ts`, and `scripts/video-review/doctor.ts` all pass `baseURL: "https://api.anthropic.com/v1"` explicitly to `createAnthropic({...})` so the env var is overridden. **Any new Claude integration must do the same.** Pattern: `const anthropic = createAnthropic({ apiKey, baseURL: "https://api.anthropic.com/v1" })`. Never use the default `anthropic` singleton from `@ai-sdk/anthropic`.
- **`groupByTime` uses hardcoded AEST (lib/operator/cases/time-grouping.ts)**: `startOfDayAEST()` shifts by +10h, floors to UTC day boundary, shifts back — independent of system timezone. Previously used `setHours(0,0,0,0)` which gave different bucketing on UTC CI runners vs local AEST macOS (PR 51 caught + fixed 5 timezone tests). Do NOT "fix" the timezone to UTC or system-local; the platform serves AU operators on AEST wall-clock days. Known limitation: AEDT (Oct-Apr daylight saving, UTC+11) shifts day boundaries by 1h. If precision matters, refactor to `Intl.DateTimeFormat` with `timeZone: "Australia/Sydney"`. Documented inline in the source.
- **Branch protection on `main`: APPLIED 2026-05-23 evening.** Verified via `gh api repos/reabal-n/instantmed/branches/main/protection`. Required check: `build` (strict — branch must be up to date before merge). `enforce_admins=false` so the owner-operator can bypass for emergencies. No PR review requirement (solo operator). To extend to also require `Doc surface audit`, change `contexts` to `["build", "Doc surface audit (count + plan-refs + 8 pinning contracts)"]`. To temporarily disable for a one-off emergency push, `gh api -X DELETE repos/reabal-n/instantmed/branches/main/protection` then re-apply with the saved JSON.
- **Operational invariants no one watches** (added 2026-05-23 evening): four SQL queries in `docs/OPERATIONS.md` §Integration Invariants + Operator Runbook Queries detect (Q1) queue P95 SLA breaches by category — med cert work was at 165h vs 24h max as of 2026-05-23 but resolved — as of 2026-06-04 med certs avg 36 mins (P95 1.5h), consults avg 7h (P95 12.8h), queue empty; (Q2) cert+refund orphans where the platform issued a cert AND refunded but didn't revoke — 2 cases as of 2026-05-23 evening; (Q3) integration env vars pointing at resources that exist but are wrong type (Google Ads conversion action ID is the active example; Stripe price IDs are the next likely place); (Q4) refund-record invariant violations (`payment_status=refunded` with no refund_status/refunded_at). The auto-revoke-on-refund question is clinical/legal policy, not engineering — do not ship a contract test that ENFORCES it without operator sign-off.
- **Med cert language is locked**: `lib/pdf/template-renderer.ts` `getBodyText` / `getReturnText` / `getSupportText` use conservative consultation-statement language only. The body has two locked paragraphs: certification (`getBodyText`; carer = "provide care and support to an immediate family or household member") and a combined closing paragraph = absence scope (`getReturnText`) + a warmth-only support line (`getSupportText` = "Please get in touch with us if you have any questions.") rendered as one paragraph. Do NOT add "medically unfit", exam-deferral support, fitness-for-driving / firearm / court / flying / custody language, or any modality disclosure ("asynchronous telehealth") in the cert body, and do NOT put "concerns" / "do not hesitate" / the support email in the support line. Marketing copy in `components/marketing/sections/certificate-type-selector.tsx` must not promise outcomes ("Accepted by all X", "Supports Y"). High-stakes use cases (exam, court, fitness-to-X, workers comp, NDIS, TAC) are blocked at intake by `checkHighStakesUseCase` in `lib/clinical/intake-validation.ts` and again at auto-approval by `HIGH_STAKES_USE_CASE_KEYWORDS` in `lib/clinical/auto-approval.ts`. Hard duration cap: `MAX_MED_CERT_DURATION_DAYS = 3`.
- **Verify lib is the single source of truth**: `lib/verify/certificate.ts` exports `verifyOutcome`, `formatDoctorName`, `formatCertificateType`, `maskPatientName`. `/api/verify` and `/verify/[certificate_ref]` both consume it — TypeScript will fail to compile if a new `CertificateStatus` is added without a branch. Doctor names always render as `Dr. {Name}, {Nominals}` (idempotent). Don't fork the formatters back into the route files.
- **Post-deploy smoke test**: `.github/workflows/post-deploy-smoke.yml` runs on every successful production deployment. Hits a canary cert (`IM-STUDY-20260426-06236622`) plus `/sign-in`, `/auth/forgot-password`, `/auth/reset-password`, `/api/auth/sign-out`, `/patient`. If the canary cert is rotated, update the workflow's `CANARY_CERT_REF` env var.
- **Tailwind v4**: CSS-first config. Custom morning spectrum colors (sky, dawn, ivory)
- **Staff cockpit shell**: Admin, doctor, and support staff screens share `OperatorShell`/`components/operator/*`. Do not reintroduce separate admin/doctor mode switching, scroll-heavy dashboard pages, or duplicate case summary layouts; put operational actions above the clinical review context. New list surfaces should use the cockpit primitives in `components/operator/cases/*` (`CaseRow`, `CaseTable`, `FilterBar`, `StatusDot`, `DensityToggle`, `QuickFilterChip`) and the `useDensity` hook from `lib/operator/cases/use-density.ts` so density, hover, status, and sort behaviour stays consistent.
- **Intake ledger (2026-05-20 rebuild)**: `/admin/intakes` is the single canonical ledger client at `app/admin/intakes/intakes-ledger-client.tsx`. Default sort is `created_at DESC`. The historical client-side status-priority sort is now an opt-in "Smart sort" toggle, not the surprise default. **Do not add an unconditional status-priority client re-sort to any new staff list surface** — make work-priority an explicit operator choice, not the silent default. The ledger keeps the existing `IntakeReviewPanel` slide-over: a plain left-click on a row opens it, Cmd/Ctrl/middle-click follows the row's `href` to the case detail page. Sort and quick-filter state mirror to the URL so views are shareable. Use `STAFF_LEDGER_HREF` for any `router.replace` targeting the ledger. **Refund visibility (2026-05-20):** the ledger row carries a `refundIndicator` chip (`Refunded` / `Partial` / `Refund failed` / `Refunding`) from `lib/operator/cases/types.ts`, and the FilterBar exposes `Refunded` + `Refund failed` quick-filter chips. Support staff can now reach this surface (page gate is `["admin", "support"]`); the row still hides clinical answers, which only render on the intake detail.
- **Status chrome on staff lists is calm, not loud (2026-05-21)**: Status indicators on staff list surfaces (intake ledger, patient list, anywhere a row needs to show a state) render as an inline 8px semantic dot + plain-text label, NOT as a colored-background pill (`bg-success-light text-success border-success-border` style). Canonical primitive: `StatusDot` from `@/components/operator/cases/status-dot.tsx`. Reserved colored-background `<Badge>` variants are still allowed for exception states the operator must act on (Refunded chip in the ledger row, Express priority on a CaseRow). **Do not reintroduce loud colored-background pills for routine status display** (Complete / Synced / Needs details etc.); reach for the dot + label pattern. The patient list `/admin/patients` and `/doctor/patients` were converted on 2026-05-21 — keep that as the reference. Same surface: stat summaries for the patient list are a single thin horizontal strip (one row of `icon + label + value + label + value...`), not a 4-card 2×2 grid. Hover-title carries the detail copy.
- **Cockpit counter + recovery primitives (2026-05-20)**: `CounterCard` (`components/operator/counter-card.tsx`) and `RecoveryRow` (`components/operator/cases/recovery-row.tsx`) are the canonical primitives for any staff surface that needs a 4-up KPI / counter strip or a scannable "what just broke" list. Both follow the calm-chrome rule (no colored-background pills on routine status). `/admin/ops` is the reference implementation; the upcoming `/dashboard` KPI strip and any future operator alert feed should consume the same primitives, not hand-roll new ones. Calm-chrome contract test: `lib/__tests__/admin-ops-calm-chrome.test.ts` (greps the 4 cockpit files for forbidden `bg-{emerald,orange,red,amber,sky}-{50,100}` with a `(?!\d)` lookahead so the legitimate 500-shade dots are not falsely matched).
- **Identity-required gate is "anything that is not a med cert" (2026-05-21)**: `requiresPrescribingIdentityForRequest` in `lib/request/prescribing-identity.ts` returns `false` ONLY for the medical_certificate / med_certs / med_cert category and service-type. Every other service (prescription, repeat_rx, every consult subtype including ones that may reactivate later — weight_loss, womens_health, infection, mental_health, general) requires the full structured identity bundle: address + Medicare + sex on `profiles`, plus phone (enforced separately by the checkout identity step). Do NOT narrow the gate back to an allowlist of consult subtypes; future re-activation of a service should inherit identity gating automatically. Pinned by `lib/__tests__/prescribing-identity-gate-contract.test.ts`. Operator-stated rule: name + DOB + email for ALL services (enforced at checkout), address + Medicare + phone for everything EXCEPT med certs. Prior bug: an allowlist of `{ed, hair_loss}` consult subtypes left general / mental_health / infection / repeat_rx stragglers with NULL profile columns; backfilled via the Supabase MCP on the same day.
- **Attribution surfacing (2026-05-21)**: `components/doctor/attribution-chip.tsx` renders the source (Google Ads / ChatGPT / Direct / etc.) for staff on the intake detail header and patient profile identity strip via `classifyAttributionSource` from `lib/analytics/source-classification.ts`. Calm chrome (8px dot + plain text), no backdrops. Patient-side surfaces never see this; it is acquisition data, not clinical.
- **`/admin/patients` accepts both admin and doctor (2026-05-21)**: Page gate is `requireRole(["admin", "doctor"])` and uses `hasAdminAccess(auth.profile)` to decide whether to pass `doctorId` into `getPatientDirectoryPage`. Admins see all patients; non-admin doctors are scoped to patients they've actually touched (same pattern as `/doctor/patients`). Both routes render the same `PatientsListClient` so any polish lands in both surfaces. Contract test `lib/__tests__/admin-navigation-contract.test.ts` has `app/admin/patients/page.tsx` in `doctorAdminSharedPages` so the strict "admin pages must use `requireRole(["admin"])`" rule has a narrow exception for this surface only.
- **`refund_status` enum does NOT include `"refunded"`**: Valid values are `not_applicable | not_eligible | pending | succeeded | failed | skipped_e2e`. The "fully refunded" state is expressed via `payment_status = "refunded"`. Writing `refund_status = 'refunded'` in SQL throws `invalid input value for enum`. Use `refund_status = 'succeeded'` when logging a completed refund.
- **Email typo domains pass silently — and relay addresses deliver invisibly**: Typosquat domains like `gamil.com` are real MX hosts, so Resend marks the send as `status: "sent"` with `delivery_status: null` and no bounce. Relay addresses go one further: Apple "Hide My Email" (`word_word.xx@icloud.com` or `@privaterelay.appleid.com`), DuckDuckGo (`@duck.com`), and Firefox Relay (`@mozmail.com`) show `delivery_status: "delivered"`, but the patient reads the forwarded copy in the inbox behind the relay — and reports "never received it" from a completely different address (2026-07-02 incident: paid med cert delivered to a Hide-My-Email address iOS autofilled at guest checkout; patient texted support from her gmail, which appears nowhere in the DB). Triage order when a patient says nothing arrived: check `email_outbox.to_email` for a typo, then for a relay pattern — `delivered` + `email_opened_at` null + relay-shaped address means "check the inbox linked to your Apple ID (and spam)", not a resend to the same address. Entry-time prevention: `lib/validation/email-typo.ts` (tap-to-fix "Did you mean gmail.com?") and `lib/validation/email-relay.ts` (expectation-setting note) both render under the intake email field (`patient-details-step.tsx`, derived from the value so autofill/prefill trigger it; the early inline recovery-email card on step 1 was removed 2026-07-08 — email is captured once, at the personal-details step). Both are informational only — relay addresses are legitimate and must never block progression or fail validation.
- **Easy refund (2026-05-20)**: `IntakeRefundDialog` at `app/doctor/intakes/[id]/intake-refund-dialog.tsx` is the canonical refund button on the intake detail page. It shows for `payment_status` of `paid` (full refund) or `partially_refunded` (top-up). Uses `TypedConfirmDialog` with `requiredText="REFUND"` and shows a concrete refund-amount preview before the operator confirms. The server action is `issueRefundAction` in `app/doctor/queue/actions.ts`: role gate is `["doctor", "admin", "support"]`; support is capped at `SUPPORT_REFUND_CAP_CENTS = $100` per refund and `SUPPORT_REFUND_MAX_PER_24H = 3` per rolling 24h via `checkSupportRefundLimits`. Top-up refunds use a per-already-refunded idempotency key (`standalone_refund_topup_${intakeId}_${alreadyRefundedCents}`) so retries are safe; the action explicitly passes `amount: remainingCents` to `stripe.refunds.create`. Recent refund activity surfaces on `/admin/ops` in the Refunds card (last 24h count + 5 most recent rows, link to ledger filtered to `chips=refunded`).
- **Staff dashboard URL (Phase 2 remaster, 2026-05-12)**: `/dashboard` is the live staff cockpit. `/admin`, `/doctor`, `/doctor/dashboard`, and `/doctor/queue` redirect here from `next.config.mjs`. New code references `STAFF_DASHBOARD_HREF` / `buildStaffDashboardHref(...)` from `lib/dashboard/routes.ts` rather than the legacy `ADMIN_DASHBOARD_HREF` / `DOCTOR_DASHBOARD_HREF` aliases. The dashboard page is role-aware: admin renders setup/readiness cards above the same clinical queue; doctor renders the queue; support is forwarded to the bounded support ops cockpit at `/admin/ops`.
- **Future doctor data boundary (Phase 7.1 remaster, 2026-05-13)**: The owner-admin remains broad. Non-admin doctors are scoped on patient directory, patient detail, patient drawer APIs, command-palette search, and doctor analytics through `lib/doctor/patient-access.ts`. A doctor relationship is concrete only when the doctor claimed/reviewed/is reviewing an intake, owns a script task, issued a certificate, or created a patient note. Do not make future doctors broad by using admin data fetches or global palette/search helpers.
- **SystemHealthPill (Phase 2 remaster)**: Header pill on `/dashboard` that polls `/api/admin/system-health` every 90s (`POLL_INTERVAL_MS` is the source of truth; the popover copy derives from it). Counts stuck intakes, webhook DLQ failures, Parchment failures, and email-outbox failures from the last 24h. Click for a popover with deep links. Failure-tolerant: keeps last-known state when the endpoint is unreachable.
- **Retired dashboard components (Phase 2 remaster)**: `AdminPulse`, `lib/data/admin-pulse.ts`, `yesterday-widget*`, `AdminHubZones`, and `IntakeMonitor` were deleted. `/admin/analytics` reads the queue-health helpers directly; do not reintroduce a separate live-monitor widget or polling API.
- **Operator Overview consolidation (2026-07-09)**: `/admin/analytics` is retitled **"Overview"** and is the single business-at-a-glance surface. The standalone `/admin/finance` ("Payments") page was folded in — it now redirects to `/admin/analytics` (`next.config.mjs`) and the "Payments" nav item was removed from `operatorNavSections` (`lib/dashboard/staff-navigation.ts`, label `Analytics`→`Overview`). Default view = **4 essentials only**: revenue windows + a 7-day net trend, the conversion funnel with the single biggest drop-off named from live PostHog step events (`pickWorstStep`), "Where patients came from" (self-report `heard_about_us` + AI-assistant UTM merged), and a **compact/calm Google Ads panel**. Everything deeper (operating scorecard, full Paid-acquisition P&L campaign table, service mix, intake-friction tables, checkout recovery, Google Ads upload health, prescription fulfilment, patients-by-state, queue health) is demoted into a collapsed `<details>` "Detailed metrics" disclosure — nothing was deleted. `getRevenueDashboard()` powers the revenue block (shared payment truth; folds in what the Payments page showed). Live indicator + 60s `router.refresh()` (visible-tab gated) via `app/admin/analytics/live-refresh.tsx`. **Google Ads IS connected and live** (GAQL reporting via `getGoogleAdsSpendAuditReport`→`searchGoogleAds`, plus upload health via `getGoogleAdsHealth`, plus PostHog HogQL for the funnel) — the panel just reads calm ("Minimal spend"/"Ads paused") under $50/30d spend so trivial-spend ROAS isn't alarming; it is NOT a broken integration. The standalone `/admin/finance` route was **fully removed** (`page.tsx`/`finance-client.tsx`/`error.tsx`/`loading.tsx` deleted, `STAFF_FINANCE_HREF` dropped from `lib/dashboard/routes.ts`); both `/admin/finance` and the legacy `/admin/finance/revenue` alias now 301→`/admin/analytics`. Refund quick-access was preserved on the Overview via `ADMIN_REFUNDS_HREF`. Analytics client heading + folded-in payment-pressure/refund pins live in `admin-navigation-contract`/`ops-dashboard-contract`; support/design-system contracts no longer reference the finance client.
- **Certificate resend (Phase 3 remaster, 2026-05-12)**: One file (`app/actions/resend-certificate.ts`) exposes two named exports: `resendCertificate(intakeId)` for the patient self-serve resend (rate-limited 5/24h via Upstash Redis) and `resendCertificateAsStaff(intakeId)` for doctor/admin/support resends (max 3 per certificate via `issued_certificates.resend_count`). Both share the same email template, lookup, and audit logging. Do not reintroduce `resend-certificate-admin.ts`.
- **CertHealthChip (Phase 3 remaster)**: `components/doctor/cert-health-chip.tsx` is the canonical at-a-glance certificate delivery badge. Renders sent/opened/failed/resent state with a tooltip and links to `/admin/emails/hub?intake=<id>` for the full email audit. Used on `intake-detail-header.tsx`; do not hand-roll inline status badges. Pulls from `getCertDeliveryStatus(intakeId)` in `lib/data/issued-certificates.ts`.
- **Certificate UI labels (Phase 3 remaster)**: `Edit & Reissue` was renamed to `Correct certificate (same reference)` everywhere it surfaces (button on the intake header AND the `CertificatePreviewDialog` title). Same reference number, replaces the PDF in-place, max 3 corrections per certificate via `reissue-cert.ts`. The historical "Replace certificate" workflow (new reference + supersedes old via a `regenerate-certificate.ts` action) was retired before the remaster — `medical-certificate-retired-paths.test.ts` enforces `regenerate-certificate.ts` does NOT exist. If a certificate is in `revoked`, `superseded`, or `expired` status today, the only path to issue a fresh certificate is to revoke (if not already) and re-approve the intake from the doctor queue. Do not reintroduce a "Replace certificate" button without first deciding whether to build the regenerate action behind it.
- **Patient profile (Phase 4 remaster, 2026-05-12)**: One canonical patient profile at `/doctor/patients/[id]`. `/admin/patients/[id]` is a redirect-only stub — the duplicate 471-line admin patient page (4-stat tile row + 3-card layout) was retired in Phase 4a. Admin-specific actions (patient merge audit, refunds) live on the operator nav, not duplicated on each profile.
- **Unified patient timeline (Phase 4b remaster, 2026-05-12)**: `components/doctor/patient-timeline.tsx` is the canonical activity stream for the patient profile. It absorbs five channels — requests, prescriptions (Parchment + InstantMed-requested), notes, emails, audit/Parchment webhook events — into one chronological list with channel filter tabs (All / Requests / Prescriptions / Notes / Emails / Audit) and "Show older" pagination. The retired layout (separate "Active Prescriptions" Card + simpler PatientTimeline + `PatientCommunicationHistory` block) was scroll-heavy and forced the doctor to triangulate between three lists. The patient profile now renders: identity strip, prescribing workspace, delivery evidence, patient summary grid, then the timeline (primary content). The merge dialog uses `TypedConfirmDialog` with `requiredText="MERGE"`. Compact mode (`compact` + `maxItems`) is preserved for the side panel and intake review cockpit callers — those callers were not changed in Phase 4b.
- **Shared staff shell (Phase 1.2)**: Both `/admin/*` and `/doctor/*` routes render through `OperatorShell` with role-aware nav from `getStaffNav(profile)`. `DashboardSidebar` was deleted from `components/shared/`; the patient portal uses `PatientShell` -> `AuthenticatedShell` and never used DashboardSidebar in production. The doctor portal layout (`app/doctor/layout.tsx`) wraps children in `DoctorShell` INSIDE `OperatorShell` and passes `hideMobileHamburger` so the bottom-tab `DoctorMobileNav` is the sole mobile nav. To add a new staff item that all roles see, edit both `operatorNavSections` AND `doctorNavSections` AND `supportNavSections` in `lib/dashboard/staff-navigation.ts`. **Nav consolidation (2026-07-12, operator request):** the "Review" and "Scripts" sidebar items were removed — they were `/dashboard?status=…` deep-links to the SAME page the Dashboard/Queue item opens, whose in-page tab strip (All / Review / Info / Scripts to write) switches those filters with zero navigation. Do NOT re-add status-filter deep-links as nav items (pinned by `admin-navigation-contract` + `support-nav-contract` + `doctor-navigation-contract`). `STAFF_QUEUE_HREF`/`STAFF_SCRIPTS_HREF` + the `/doctor/queue`/`/doctor/scripts` redirects stay for old bookmarks, the user-menu, and onboarding links. Same change restored **default (auto) Link prefetch** on the staff sidebar — on our force-dynamic staff routes auto-prefetch warms only the layout + `loading.tsx` skeleton, never the PHI page payload; FULL prefetch (`prefetch={true}`/`router.prefetch`) stays contract-banned. `getAuthenticatedUserWithProfile` is React `cache()`-memoized so the layout+page `requireRole` pair costs one auth+profile+decrypt per navigation, and the ledger rows now carry a calm acquisition-source label (`buildCaseRowAttribution` in `lib/operator/cases/case-attribution.ts` — classifier group + short label, `heard_about_us` self-report standing in for dark Direct/Unknown rows).
- **`support` role**: Non-clinical operations role. Added to `user_role` enum on 2026-05-11. Phase 7 of dashboard remaster (2026-05-12) initially bounded support to `/admin/ops`, `/admin/webhook-dlq`, `/admin/ops/parchment`, and `/admin/ops/prescribing-identity`. **Phase 8 (2026-05-20)** opened `/admin/intakes` (intake ledger) and the intake refund action to support — they need to see paid/refunded state and process refund tickets without escalating every case to admin. Support still sees only ledger metadata (no clinical answers, no email hub, no settings, no finance, no analytics, no clinical approvals, no prescribing); the intake detail page still gates clinical surfaces. Refunds are capped at $100/refund and 3 per rolling 24h via `checkSupportRefundLimits` in `app/doctor/queue/actions.ts`. Application-layer checks via `hasSupportAccess(profile)` from `lib/auth/staff-capabilities.ts`.
- **Per-doctor capability flags**: Seven boolean columns on `profiles` (`can_review_med_certs`, `can_review_repeat_rx`, `can_review_consults`, `can_review_ed`, `can_review_hair_loss`, `can_prescribe_s4`, `can_prescribe_s8`). All default true except `can_prescribe_s8` (explicit grant required). Owner-operator gets all flags true. Check via `doctorHasCapability(profile, "review_ed")` from `lib/auth/staff-capabilities.ts`. Phase 7 of dashboard remaster (2026-05-12) wired the runtime gates: `app/actions/approve-cert.ts` gates on `review_med_certs` before the rate limit / credential checks; `app/doctor/queue/actions.ts` -> `updateStatusAction` gates on `doctorCanReviewService(profile, serviceType, subtype)` for `approved` and `awaiting_script` transitions, and `declineIntakeAction` gates BEFORE delegating to the canonical decline (so a wrong-line doctor cannot trigger refund + email). The mapping lives in `requiredCapabilityForService(serviceType, subtype)` and `describeServiceCapability(...)` provides the user-visible label for denial errors. Parchment handoff also gates on `prescribe_s4` for every embedded prescribing launch and `prescribe_s8` when repeat-script intake answers contain a controlled-medication name, via `checkParchmentPrescribingCapability(...)` in `lib/doctor/parchment-prescribing-capability.ts`.
- **Typed-confirm destructive actions**: `components/ui/typed-confirm-dialog.tsx` is the canonical primitive for hard-to-undo operator actions. The continue button stays disabled until the operator types the `requiredText` token (case-sensitive by default, case-insensitive opt-in via `caseInsensitive`). Use a fixed verb (`REFUND`, `REVOKE`) for fixed-class actions, and a dynamic identifier (patient name, intake ref) when the goal is to catch "wrong tab" mistakes. Wired today on the intake refund dialog (`app/doctor/intakes/[id]/intake-refund-dialog.tsx`). When adding a new destructive action (cert revoke, account closure, manual paid mark, bulk decline, patient merge), reach for this primitive instead of a plain "Are you sure?" `AlertDialog` so the friction is consistent across the cockpit. Contract: `lib/__tests__/typed-confirm-dialog-contract.test.ts`.
- **Test-data toggle (admin-only)**: `/dashboard?showTestData=1` opts the staff queue in to seeing the seeded E2E patient (`SEEDED_E2E_PATIENT_PROFILE_ID`). Gated on `hasAdminAccess(profile)` so a doctor with a copy-pasted URL cannot flip the visibility. The toggle button + amber "Test data visible" banner live in `components/operator/test-data-banner.tsx` and are exposed via `@/components/operator`. Under the hood, `filterSeededE2EIntakes(query, { allowSeeded })` accepts an explicit override; `getDoctorQueue({ allowSeeded })` is the first caller threaded through (Phase 1 of test-data toggle, 2026-05-12). New surfaces opt in the same way — pass `allowSeeded` through their data fetch options. The env-based fallback (`PLAYWRIGHT`, `E2E`, `E2E_MODE`, `NODE_ENV=test`) still wins automatically; only production sessions need the query param. Contract: `lib/__tests__/test-data-toggle-contract.test.ts`.
- **Route group conflicts**: Never place `page.tsx` inside a route group `(name)/` if the parent dir also has `page.tsx` — both resolve to the same URL and Vercel's build tracer will fail with ENOENT. CI runs `scripts/check-route-conflicts.sh` to catch this
- **Canonical intake flow**: `/request` is the **sole** canonical intake. `/start` is a compatibility handoff for old marketing/email links and must 307 redirect into `/request` while mapping legacy `service` values and preserving attribution/query params. The `/flow` parallel system was deleted in 2026-04-08 (commit `18e26f0b7`). `/flow` and `/flow/:path*` now 301 to `/request` via `next.config.mjs`. Do not add any new logic under a `/flow/*` path, and do not reference `lib/flow/*` — safety engine lives at `lib/safety/` and offline queue at `lib/offline-queue.ts`.
- **Redirect-only pages**: `/prescriptions/request`, `/prescriptions/new`, `/prescriptions/repeat`, `/consult/request`, `/medical-certificate/request`, `/medical-certificates/*`, `/medications/*`, `/login`, and `/auth/login` redirect to canonical routes via `next.config.mjs`. The duplicate `page.tsx` trees were removed; do not recreate local client/action files for those aliases.
- **Google indexing triage**: Use `pnpm seo:gsc-index-audit -- --inspect-limit=20` for read-only Search Console URL Inspection audits. Ignore expected blocked/not-indexed noise for `_next/static`, fonts, favicon/manifest, auth/account, Clerk, and redirect-only aliases. Depth work belongs only on public canonical URLs that should rank; IndexNow submits every robots-advertised sitemap URL for Bing/Yandex discovery only and does not fix Google indexing.
- **Stack pin check regex**: `scripts/check-stack-pins.sh` uses glob `[[ == *"^"* ]]` substring tests, NOT bash regex bracket classes (`[[ =~ [\^~\>*x] ]]`). The regex form is bash-version-dependent and produces false positives on bash 5 (Ubuntu CI). Fixed in commit `1e54d69ee` — don't revert it.
- **Orphaned file check**: `scripts/check-orphaned-files.sh` detects dead intake steps not in the step registry, stale `/flow/` routes, `@deprecated` modules with zero imports, and orphaned worktree directories. Run it before deploying.
- **AI worktrees live outside the repository root**: Use sibling worktrees such as `/Users/rey/Developer/instantmed-worktrees/<branch-slug>` for Codex/Claude implementation branches. Do not create nested `.worktrees/` inside this repo unless explicitly requested; nested lockfiles confuse Next.js output-file tracing and produce false workspace-root warnings. Remove merged worktrees promptly.
- **CI runs blocking focused E2E smoke** via `.github/workflows/ci.yml` on every push/PR to `main`: ops/navigation/clinical-input safety (`e2e/admin.ops-index.spec.ts`, `e2e/marketing-dashboard-nav.spec.ts`, `e2e/dashboard.keyboard-safety.spec.ts`) plus paid critical flows (`pnpm medcert:readiness:e2e`, `e2e/unified-request-flow.spec.ts`, `e2e/consult-subtypes.spec.ts`, `e2e/parchment-webhook.spec.ts`). Requires repo secrets `E2E_SECRET`, `ENCRYPTION_KEY`, Supabase URL/anon/service-role keys, `STRIPE_WEBHOOK_SECRET`, and `PARCHMENT_WEBHOOK_SECRET`; CI fails fast if any required E2E secret is missing. Later E2E gates still run for full signal when an earlier E2E step fails, and the E2E job is serialized with `instantmed-shared-e2e-fixtures` because the blocking specs reuse deterministic Supabase seed fixtures. The older broad Playwright suite is currently non-blocking debt because it contains stale routes/product assumptions and timeouts.
- **AGENTS.md is generated from CLAUDE.md**: `scripts/sync-agent-doc.sh` projects CLAUDE.md -> AGENTS.md (Claude vs Codex naming, tool-config paths). CI runs `scripts/sync-agent-doc.sh --check` so drift fails the build. **Edit CLAUDE.md, run the script, commit both.** Never edit AGENTS.md by hand.
- **Analytics & attribution pipeline**: Google Ads (`AW-17795889471`) + GA4 (`G-X0QJQRLL2Y`) IDs in `lib/analytics/google-tag-ids.ts`. Capture flow: ad URL params -> `captureAttribution()` (`lib/analytics/attribution.ts`) + middleware cookie writer `captureAttributionToCookie()` (`lib/analytics/middleware-attribution.ts`) -> sessionStorage + first-party cookie -> `resolveCheckoutAttribution()` server-side -> `intakes` columns (`utm_*`, `gclid`, `gbraid`, `wbraid`, `campaignid`, `adgroupid`, `keyword`, `creative`, `matchtype`, `device`, `network`). **Multi-channel:** non-Google ad clicks (`fbclid`/`msclkid`/`ttclid`/`li_fat_id`) are mapped to a `utm_source`/`utm_medium` pair at capture by `deriveChannelFromClickIds()` (`lib/analytics/click-id-channels.ts`) so the order classifies as `other_paid` not `direct`. The raw non-Google click id is NOT persisted (no DB column; `normalizeAttribution`'s whitelist in `lib/analytics/server-attribution.ts` only keeps keys in `ATTRIBUTION_TEXT_LIMITS`, which maps 1:1 to the columns above — a Meta/TikTok offline-conversion-API integration would need a column-adding change first). Client gtag purchase fires in BOTH `app/patient/intakes/success/success-client.tsx` AND `app/auth/complete-account/complete-account-form.tsx`; guest checkouts only hit the latter. Server-side Google Ads Conversion API v24 (`lib/analytics/google-ads-conversion-api.ts`) fires from the Stripe webhook when `intakes.gclid` is present; Google dedups on `orderId` so client + server safely double-fire. **Enhanced conversions (added 2026-06-12; ENABLED in the Ads UI — method "Google Ads API" + customer-data terms accepted):** the server upload also attaches SHA-256 hashed customer **email + phone** (`userIdentifiers` / `FIRST_PARTY`, one object per identifier — it's a protobuf oneof). `runGoogleAdsPostPaymentAttribution` resolves them from the **patient profile** via `resolveEnhancedUserData(supabase, intakeId)` (email is plaintext; phone is decrypted from `phone_encrypted` via `decryptField`), so BOTH the live webhook and the cron-backfill path carry user data — the webhook call sites no longer pass a Stripe-session email. Normalization + hashing live in `hashEmailForGoogleAds`/`normalizeEmailForGoogleAds` (lowercase + trim, gmail/googlemail drop local-part dots) and `hashPhoneForGoogleAds`/`normalizePhoneForGoogleAds` (E.164, bare national numbers default to +61), lowercase SHA-256 **hex** — must match Google's algo or the match silently fails. Resolver is fail-soft (`{}` on any error, never blocks an upload). Kill switch `GOOGLE_ADS_ENHANCED_CONVERSIONS_DISABLED=true` → gclid-only. Only the `has_user_data` boolean is logged to audit_logs/PostHog — never the email/phone or hash. Server PostHog captures carry `is_e2e` via `getPostHogBaselineProperties()` in `lib/analytics/posthog-server.ts`; **always filter `is_e2e=false`** in funnel and revenue queries. Account-level config (IDs, Final URL suffix, conversion action ID, pending Ads UI actions) lives in memory `project_google_ads.md`.
- **Checkout price-config failures always alarm + stay recoverable (added 2026-06-27)**: A broken/missing `STRIPE_PRICE_*` env makes `getPriceIdForRequest()` (`lib/stripe/price-mapping.ts`) **throw** — either Stripe's "No such price" (env points at a deleted/wrong price) or our own "Missing STRIPE_PRICE_* environment variable" (env absent). Both are the same catastrophe class as PR #105: EVERY checkout for that tier dies until a human fixes the env. `lib/stripe/checkout-error-alarm.ts` `reportCheckoutSessionFailure()` now classifies BOTH as fatal (`isMisconfiguredPrice`) with distinct tags/fingerprints (`no_such_price` → `["stripe-no-such-price", role]`; `missing_price_env` → `["stripe-missing-price-env", role]`); generic session-create failures stay `error`-level. Wired into all three checkout paths so a price throw can never vanish into a generic outer catch: **guest** (`lib/stripe/guest-checkout.ts`) wraps the early lookup → null, persists a recoverable `checkout_failed` intake, then alarms; **authed** (`lib/stripe/checkout.ts`) catches → alarms + graceful return (no intake row yet, so the alarm is the recovery signal); **retry** (`lib/stripe/checkout/retry-payment.ts`) alarms in the session-create catch (previously returned a user message with NO Sentry trace) AND emits `payment_initiated` so retried checkouts count in the funnel's trustworthy server denominator (see #200). Guarded by `lib/__tests__/checkout-error-alarm.test.ts` + `checkout-price-config-alarm-contract.test.ts`.
- **Guest complete-account confirms payment from Stripe, not the lagging DB column (added 2026-06-27)**: `/auth/complete-account` (`app/auth/complete-account/page.tsx`) populates amount/service + fires the client gtag purchase for guest checkouts (they never hit `/patient/intakes/success`). It must NOT hard-filter `payment_status = "paid"` — the Stripe webhook that flips that column lags the redirect to this page, and the old filter hid amount/service + skipped the conversion fire during that race (Codex review 2026-06-27). The page now requires the URL `session_id` to match the intake's stored `payment_id` (a high-entropy proof the visitor is the paying customer — strictly stronger than the old paid-only filter for not exposing order data to a bare `intake_id`), then confirms paid from the column OR directly via `stripe.checkout.sessions.retrieve()` (fail-soft helper `isStripeSessionPaid`; any error → not-confirmed, never breaks the page). Keep the `session_id` match if you touch this query. Related: the `checkout_failed` PostHog event from the unified review/pay surface now carries a `reason` (the real error string, or `missing_checkout_url`) so the failure breakdown is actionable instead of one opaque bucket; the failure Alert also scrolls into view + offers a `support@` recovery link so it isn't missed on mobile.
- **Google Ads server-side conversion-action alarm (added 2026-06-08)**: A misconfigured `GOOGLE_ADS_CONVERSION_ACTION_PURCHASE` (an id not found in the Ads account) makes EVERY server-side upload fail with `NO_CONVERSION_ACTION_FOUND` until a human fixes the env — exactly the Stripe "No such price" failure class (PR #105), and it actually happened **May 19 → Jun 1 2026** (76 failed uploads, Smart Bidding optimised blind on wasted spend; the hourly backfill cron only `logger.warn`'d the bad preflight and swallowed it). `lib/analytics/google-ads-conversion-alarm.ts` mirrors `lib/stripe/checkout-error-alarm.ts`: `classifyGoogleAdsConfigFailure()` flags config-class codes (`conversion_action_not_found/_not_enabled/invalid_conversion_action_type/missing_env/no_access_token`, plus raw `NO_CONVERSION_ACTION_FOUND`/`INVALID_CONVERSION_ACTION_TYPE` upload strings) and `reportGoogleAdsConversionFailure()` escalates them to a Sentry **`fatal`** with a stable `["google-ads-conversion-action", code]` fingerprint (one alert per config break, not one per order). Wired into BOTH the cron preflight skip branch (`app/api/cron/google-ads-conversions/route.ts`) and the webhook upload path (`lib/analytics/google-ads-post-payment.ts`). Live failures surface on `/admin/ops` ("Google Ads conversions" counter, last 7d, via `getGoogleAdsConversionUploadHealth()`). Transient/per-row errors are NOT config failures and keep their existing `error`-level reporting. Guarded by `lib/__tests__/google-ads-conversion-alarm.test.ts` + `google-ads-upload-audit.test.ts`.
- **Google Ads upload-audit reconciliation is PRODUCTION-scoped + upload-stream stall monitor (added 2026-06-30)**: A local `pnpm dev` server pointed at the **prod Supabase service-role key** (receiving prod Stripe webhooks) writes `google_ads_conversion_upload` audit rows into prod `audit_logs` tagged `runtime_source: "node"` / `node_env: "development"` / `status: skipped_missing_env`, with a **NULL `intake_id` column** (their intake lives in a different DB). `getGoogleAdsUploadAuditReconciliation` (`lib/analytics/google-ads-report.ts`) had no `runtime_source` filter, so ~822 dev rows counted as `orphanRows` and fired the **critical `google_ads_upload_audit_source_anomaly` Telegram+Sentry alert every ~4h for 10+ days** — a pure false alarm. Fix: reconciliation now drops `runtime_source === "node"` rows, mirroring the `/admin/ops` card filter at `getGoogleAdsConversionUploadHealth()` (`lib/analytics/google-ads-health.ts`); genuine prod orphans (`vercel` rows, no valid intake join) are still detected. **Do NOT remove the node filter from any read over `google_ads_conversion_upload` rows** — reconciliation, the ops card, and the new stream-health reader are all production-scoped. Separately, the business-alerts cron now also runs `buildGoogleAdsUploadStreamStalledAlert` (`lib/monitoring/google-ads-purchase-import-health.ts`, fed by `getGoogleAdsUploadStreamHealth`): pages `google_ads_conversion_uploads_stalled` (critical) when reportable paid orders exist but ZERO successful prod uploads reached Google in 3d — the "absence of success" detector that catches a Data-Manager-only stall the click-id-gated `purchase_imports_zero` alert is structurally blind to (DM enhanced-conversions match on hashed email/phone, no click id). The stale legacy `OfflineUserDataJob` `2265599116648626375` (EXPIRED_EVENT history) is historical only — never set it as a diagnostics-watch identifier; a live Data Manager `requestStatus=SUCCESS` is canonical and isolated from it (`statusFromDataManagerRequestStatus`: SUCCESS→accepted, FAILED/PARTIAL_SUCCESS→rejected). New metric registered in `trackBusinessMetric` (`lib/analytics/posthog-server.ts`). Guarded by `lib/__tests__/google-ads-{report,upload-audit,purchase-import-health}.test.ts`. Full proof chain + dev-noise root cause in docs/OPERATIONS.md §Google Ads Data Manager Rollout.
- **Google Ads refund retractions: grace window + DM preflight, NOT "route via DM" (added 2026-07-02)**: The Data Manager API has **no adjustment/retraction surface** (no `events:remove`; verified against the v1 reference + release notes) — do not attempt to send retractions "via the DM API". DM-uploaded conversions are retracted through the same legacy `ConversionAdjustmentUploadService`, matched by order id (the documented mechanism regardless of upload surface). `lib/analytics/google-ads-conversion-adjustments.ts` semantics: `CONVERSION_NOT_FOUND` is **transient while the latest successful upload is younger than `GOOGLE_ADS_ADJUSTMENT_CONVERSION_MATCH_GRACE_HOURS` (72h)** — Google processes uploads asynchronously and the standard same-day decline-refund otherwise fires the retraction before the conversion is queryable; an immediate-terminal classification would permanently skip it and leave a refunded ad-click conversion counted (the exact poisoning the $25/day ads validation cares about). Past the window it's `terminal_failed` (Google's meaning: never imported, or imported-then-discarded — i.e. nothing is counted). For DM uploads the stored audit `upload_identifier` is preflighted via `requestStatus:retrieve`: ingest `FAILED` → immediate terminal `dm_request_rejected` (conversion never landed; nothing to retract; no alarm), `PROCESSING` → transient wait without burning an Ads-API attempt, lookup errors fail-soft to proceed. A terminal retraction failure on a **click-attributed** upload (`has_gclid`/`has_gbraid`/`has_wbraid` on the upload audit row) fires ONE fingerprinted Sentry error per intake (`google-ads-retraction-terminal`) — the only detectable signature of "refunded order may still be counted in Google"; user-data-only uploads terminal silently (unmatched enhanced conversions never counted — all 6 stuck intakes in the 2026-07 INSTANTMED-4P incident were this class, so no actual poisoning had occurred). `skipped_missing_successful_upload` audit rows are deduped to one per intake (was 24 junk rows/day via the hourly cron). Guarded by `lib/__tests__/google-ads-conversion-adjustments.test.ts`.
- **auth-handoff analytics gate**: `lib/navigation/auth-handoff.ts` emits a browser event for observability, but it must not import PostHog or capture analytics by default. Add analytics capture only if the dashboard first-click bug recurs with evidence (issue, trace, or production log) and the event name/payload are documented in this Gotchas section and memory `project_google_ads.md`.
- **Self-reported attribution ("How did you hear about us?")**: The only way to attribute referrer-stripped dark traffic (native LLM apps, in-app browsers, iOS, word-of-mouth), which is ~50% of paid orders (the Direct/Unknown bucket) — code-side referrer capture is structurally blind to it and is already maxed out (do not add more referrer/click-id capture). One nullable column `intakes.heard_about_us` (enum token `ai|search|friend|forum|ad|other`, NOT PHI, write-once). Single source of truth for the option set + Zod enum: `lib/analytics/heard-about-us.ts`. Write path is token-authed (no auth cookie on the surfaces): `lib/crypto/heard-about-us-token.ts` (HMAC, `INTERNAL_API_SECRET`, 30-day TTL) → `app/api/attribution/heard/route.ts` (POST in-app, GET one-click for email → redirects to `/heard-thanks`) → `lib/analytics/record-heard-about-us.ts` (write-once: only sets when currently NULL). Rendered on THREE surfaces sharing the one option set: `components/patient/heard-about-us-card.tsx` on `/patient/intakes/success` (logged-in) AND `/auth/complete-account` (guest — the Direct/Unknown cohort, non-negotiable), plus one-click links in the day-2 review-request email (`lib/email/components/templates/review-request.tsx`). Post-payment only = zero checkout-conversion risk. PostHog events: `heard_about_us_shown` / `heard_about_us_answered` (measure the answer rate). It is forward-only for new orders. **`/admin/ops` breakdown strip (SHIPPED):** `getHeardAboutUsBreakdown()` in `lib/admin/heard-about-us-breakdown.ts` renders a calm thin "How did you hear about us? (30d)" strip on `/admin/ops` (per-source counts + answer rate; seeded-E2E/excluded filtered). **One-time backfill (SHIPPED):** to retro-attribute historical Direct/Unknown buyers, `lib/email/heard-about-us-backfill.ts` (finder dedups per patient, email_outbox dedup so re-runs are safe, marketing-consent gated) + the standalone `HeardAboutUsAskEmail` template (reuses the shared `lib/email/components/heard-about-us-links.tsx`, also used by the review email) sent via the admin route `app/api/admin/heard-about-us-backfill` (POST, **dry-run by default**; `{"dryRun":false,"limit":N}` to actually send). EmailType `heard_about_us_backfill` ∈ `MARKETING_EMAIL_TYPES`. Guarded by `lib/__tests__/heard-about-us-{token,options,breakdown,backfill}.test.ts` + `record-heard-about-us.test.ts`.
- **Telemetry init is gated behind first interaction EXCEPT on post-conversion pages**: `onFirstInteraction()` defers Sentry/PostHog/session-replay init to protect mobile LCP/TBT on acquisition + `/request`. But the success (`/patient/intakes/success`) and guest account-link (`/auth/complete-account`) pages fire `purchase_completed` and the gtag purchase — a user who lands and bounces without clicking would never be measured. The single source of truth for these paths is `isPostConversionPath()` in `lib/browser/post-conversion-path.ts`, consumed by `instrumentation-client.ts` (via `startTelemetryWhenReady`), `components/providers/google-tags.tsx`, and `components/providers/global-deferred-clients.tsx`. **Never wrap PostHog/Sentry init back in a bare `onFirstInteraction()` without the post-conversion bypass** — commit `ffd4a714e` did exactly that and silently killed the client purchase pixel (fired for ~6 of 204 orders over 90d) until the audit caught it. Guarded by `lib/__tests__/post-conversion-path.test.ts`. Note: the npm `posthog-js` module does NOT attach to `window.posthog`; verify init via the `POST /ingest/s/` network call, not a `window` global.
- **Intake funnel analytics (2026-06-15)**: the intake flow emits PostHog step-friction events from `components/request/hooks/use-flow-analytics.ts` + `lib/hooks/use-step-validation-summary.ts` (built by `lib/analytics/intake-events.ts`): `intake_started` (latched once per flow — does NOT re-fire on back-nav to step 1), `step_viewed`, `intake_continue_clicked`, `intake_validation_blocked` (now carries `subtype` for every consult subtype), `step_completed` (fires only AFTER the step actually advances, never on a safety-blocked Continue), `intake_answer_changed`, `checkout_viewed`. **No PHI** — payloads carry only service_type/subtype/step_id/step_index/enum tokens/durations/counts; free-text + identity answer values are forced to `value_shape:'redacted'`, never the raw value. The admin friction panel (`/admin/analytics`) aggregates via `lib/analytics/intake-funnel-summary.ts` + `lib/analytics/posthog-intake-funnel.ts` (HogQL); the paid stage keys off the server-side `purchase_completed_server` event, whose `service_category`/`service_subtype` are coalesced to the client `service_type`/`subtype` vocabulary in the query (`medical_certificate`→`med-cert`; non-consult subtypes nulled). **Always filter `is_e2e != true`** in these queries. Event names are pinned in `INTAKE_FUNNEL_EVENT_NAMES`/`POSTHOG_INTAKE_FUNNEL_EVENT_NAMES` (derived via spread so emit + query cannot diverge). Guarded by `lib/__tests__/intake-analytics-events.test.ts` + `intake-funnel-summary.test.ts`.
- **Patient profile completeness meter (2026-05-21)**: `components/patient/profile-completeness-meter.tsx` is the canonical nudge on the patient dashboard. Source of truth for completeness math: `lib/data/patient-completeness.ts` (`computePatientProfileCompleteness(profile)`). Self-hides when all 10 fields are filled. Click target is `PATIENT_SETTINGS_HREF`. Field set: full_name, date_of_birth, email, phone, sex, address_line1, suburb, state, postcode, medicare_number.
- **Renewal badge on queue rows (2026-05-21)**: `lib/doctor/renewal-detection.ts` is the canonical helper. A prescription intake is a "renewal" iff the patient has a prior `prescriptions` row with the same `medication_name` (case-insensitive) and `status` IN (active, completed). The badge surfaces on `CaseRow` (admin ledger) next to Express/Refunded chips and on the doctor queue row alongside Priority/Returning. Detection is batched per page (one extra Supabase query per `getDoctorQueue` / `getAllIntakesForAdmin` call, not per row) and fail-soft. Informational only; does NOT change the approve flow or auto-fill Parchment. `CaseRowData.isRenewal` is a required field; every new mapper must set it.
- **Doctor capability flag UI (2026-05-21)**: `/admin/doctors` now exposes the 7 capability flags via `updateDoctorCapabilitiesAction` in `app/actions/admin-doctor-capabilities.ts`. Owner-operator defaults to all 7 true; new doctors default to all true EXCEPT `can_prescribe_s8` (explicit AHPRA-restricted grant). Changes are audit-logged with the full before/after diff under the `doctor_capabilities_updated` action. Runtime gating is unchanged - `doctorHasCapability` and `requiredCapabilityForService` continue to enforce per-service flags before approvals + Parchment handoff.

---

## Operator Persona

Technical co-founder, 15+ years in health/medtech startups (AU + international). YC methodology — build fast, talk to users, iterate. Revenue and traction over perfection. Call out hills not worth dying on.

**Regulatory:** TGA/SaMD, AHPRA obligations, Medicare/telehealth rules, medico-legal risk. Know the lines; use them as advantage. No moralizing, no unsolicited caveats.

**Code standards:** TypeScript always. Zod on all API inputs. Flag PHI/RLS issues unprompted. Clean > clever. Errors to Sentry, analytics to PostHog. E2E tests non-optional for auth, payment, document flows.

**Communication:** Lead with the answer. Short sentences. Numbers over qualifiers. Bold key decisions. Flag bad ideas directly — don't soften it.

---

## Doc Maintenance Policy

**Rule: update the relevant .md file in the same commit as any platform change.** If a change isn't in the docs, the next dev/AI will make the same discovery from scratch.

| Change type | File to update | What to change |
|-------------|---------------|---------------|
| New API route | `docs/ARCHITECTURE.md` | Add to API Routes table |
| New DB table or column | `docs/ARCHITECTURE.md` | Core Tables + PHI inventory if applicable |
| New cron job | `docs/OPERATIONS.md` | Cron Jobs Reference table |
| New or changed env var | `docs/OPERATIONS.md` + `docs/SECURITY.md` | Env vars list + Secret Management |
| New kill switch | `docs/SECURITY.md` | Kill Switches table + Emergency Runbook |
| New RLS policy | `docs/SECURITY.md` | Table Policies section |
| New auth pattern | `docs/SECURITY.md` | Auth Patterns table |
| New clinical rule | `docs/CLINICAL.md` | Relevant section |
| Consent or privacy change | `docs/CLINICAL.md` | Consent Requirements or APP table |
| New service type or pricing | `AGENTS.md` + `CLAUDE.md` | Pricing table + Key Workflows |
| New intake step | `docs/ARCHITECTURE.md` | Intake System step table |
| New E2E helper / test seam | `docs/TESTING.md` | E2E Seams or relevant section |
| New component pattern | `docs/ARCHITECTURE.md` | Component Patterns section |
| Supabase migration applied | `AGENTS.md` + `CLAUDE.md` | Increment migration count |
| AI model or temperature change | `docs/ARCHITECTURE.md` | AI Configuration table |
| Refund or billing logic change | `docs/ARCHITECTURE.md` | Decline & Refund Flow |
| New third-party processor | `docs/CLINICAL.md` | Third-Party Data Processors table |
| Security incident or breach | `docs/SECURITY.md` + `docs/OPERATIONS.md` | Incident classification + response runbook |
| Attribution / analytics tracking change | `AGENTS.md` + `CLAUDE.md` Gotchas + memory `project_google_ads.md` | Pipeline summary, new event names, conversion-action IDs, URL suffix params |

**Shortcut:** Not sure which file? Ask: "Which satellite doc covers this domain?" The answer is in the Satellite Documentation table at the top of this file.

---

## Discovery Protocol

Before any non-trivial task, run a discovery phase. Read relevant code/docs first — recommendations must be grounded in what exists, not assumptions.

**Task tiers:**
- **Tier 1 (Trivial):** Single-file, obvious fix, explicit instruction. Skip discovery. → Sonnet 4.6
- **Tier 2 (Clear):** Scoped with some choices. Ask ≤3 targeted questions, brief plan, execute. → Sonnet 4.6
- **Tier 3 (Ambiguous):** Multi-faceted, vague, touches UX/architecture/strategy. Full discovery — structured plan, wait for approval. → Opus 4.6

If in doubt, tier up. "Make it better" / "fix this" without a clear definition of done = Tier 3 automatically.

**Lead with your recommendation** before asking questions. Every question must include your preferred answer first, marked "(Recommended)".

**Pushback levels:** Soft flag (product/UX/arch preference — state once, defer if overruled) · Hard stop (security/PHI/legal — don't proceed until resolved).

**Tier 3 plan format:** Goal · Approach · Decisions · Out of scope · Risks. Wait for approval before executing.

**Modes:** Advisor (strategic questions → recommendation + rationale, no code unless asked) · Executor (build/fix → read first, let the diff speak).

**Anti-patterns:** Don't paraphrase the task as a question. Don't present equally valid options when you prefer one. Don't start coding while planning to ask. Don't hedge without giving an actual recommendation.

**Proactive recommendations:** While executing a task, surface anything you would do differently or better as a separate `**I recommend:**` bullet at the end of the response. This includes adjacent code that looks wrong, naming you'd change, abstractions that should collapse, missing tests, brittle patterns, or a simpler approach you noticed mid-task. State the recommendation, the reason, and the cost. Do not silently act on it; the user decides whether to fold it in. If you notice unrelated dead code (unused imports, dead branches, orphaned files), mention it as an `**I recommend:**` bullet — never delete pre-existing dead code unless explicitly asked.
