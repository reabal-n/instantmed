# Raw browser evidence is large and reproducible from the journey definition.
# Commit curated reports, screenshots, and structured DOM/network summaries.
# Keep recordings, HARs, and traces in local or expiring CI artifact storage.
*.webm
*.har
trace.zip

# Raw Gemini responses kept only for ad-hoc debugging when JSON parse
# fails. Do not commit transient debug artefacts.
critique.raw.txt
