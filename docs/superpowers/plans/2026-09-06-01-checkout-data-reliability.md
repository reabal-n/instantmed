# Session 1 — Checkout, data and monitoring reliability

> **For agentic workers:** Use `superpowers:executing-plans` task by task. Work only this session plan. The [ROADMAP session protocol](../../ROADMAP.md#sequential-build-session-protocol) owns release and handoff requirements.

**Goal:** Remove the demonstrated checkout recovery failure, safely prepare and complete the historical encryption repair, and make monitoring report actual execution and actionable incidents.

**Architecture:** Preserve the existing checkout orchestrators, payment lifecycle guards, encryption systems and monitoring stores. Make separate reviewed changes for checkout, data repair and monitoring; do not combine them into an architectural rewrite.

**Tech stack:** Existing pinned Next.js 15.5, React 18, TypeScript, Supabase, Stripe, Vercel and GitHub Actions; Node 24 and corepack pnpm 10.23.0.

**Spec:** [ROADMAP](../../ROADMAP.md), rank 1; [reconciled audit](../../audits/2026-09-04-scaling-audit.md); [payment operations](../../OPERATIONS.md); [security](../../SECURITY.md). Operator selected reliability before certificate revenue on 6 September 2026.

**Status: Plan 1 build and release complete; operational observations remain open.** The approved review corrections are released in PRs #532–#534, with independent review, required CI, matching production deployments and scoped runtime verification below. The production profile repair and zero-change verification remain complete, and the navbar phone removal is released in [PR #530](https://github.com/reabal-n/instantmed/pull/530). Actual scheduled browser execution recovered at 12:45 UTC on September 7; the native observer cleared staleness at 12:50 and remained clear after the final release. The measured 8-hour 7-minute gap still prevents accepting the requested two-hour cadence as dependable. Retain the September 8 cadence and normal review-request observations and the unverified provider-delivery evidence. Plan 2 has not started.

## Global constraints

- Load the incident, checkout/payment, clinical-safety and doc-drift skills before their corresponding edits; UI verification applies to any changed patient flow.
- Do not make cancelled requests payable, remove uniqueness constraints, relax prescribing identity or clinical validation, or reuse a stale Stripe session.
- No patient send, refund, clinical outcome, certificate restoration, Ads mutation or encryption-key rotation is part of this plan.
- A production backfill needs an exact reviewed dry-run packet and applicable operator authorization before its apply step. Preparing the packet and hardening/testing the script are authorized implementation work; elapsed time is not authorization.
- Keep raw values, ciphertext, contact details, identifiers and SQL error details out of logs, screenshots, commits and receipts. Report aggregate counts and bounded technical codes.

## Evidence and uncertainty

At 12:18 Sydney on 6 September, one restored repeat-prescription draft emitted `checkout_v2_20260905 / persistence`, followed by abandonment. The linked intake was cancelled, failed/unpaid and had an existing payment session. No subsequent payment was observed through the 14:42 review. This correlation does **not** establish the failing branch: profile writes, duplicate handling, intake insertion and answers persistence can all return this category.

The same review counted missing encrypted copies for 417 DOB, 278 phone and 18 Medicare fields among reportable patient profiles. Counts overlap. Four new profiles checked had their expected encrypted copies. Refresh these dated counts before repair.

Source review found three prerequisites in `scripts/encrypt-phi-backfill.ts`: its key check only round-trips new local ciphertext, updates are guarded only by profile ID, and errors print profile IDs. Do not blindly run the current script.

## Task 1 — Reproduce and repair checkout recovery

**Files:** `lib/stripe/guest-checkout.ts`, `lib/stripe/checkout/persistence.ts`, `lib/stripe/checkout-submission-key.ts`, `lib/stripe/payment-integrity.ts`, `lib/request/server-draft-conversion.ts`, `components/request/steps/review-step.tsx`, `components/request/store.ts`. Inspect `supabase/migrations/20260723063000_suppress_recovery_after_intake_creation.sql` for the existing unique flow constraint; do not remove it.

**Existing verification:** `lib/__tests__/checkout-submission-key.test.ts`, `lib/__tests__/checkout-resume-payment-safety.test.ts`, `e2e/prescription-flow.spec.ts`. Add a dedicated restored-draft regression spec if the existing fixtures cannot express the failure clearly.

**Interface:** Keep `CheckoutFailureResult` and its fixed taxonomy authoritative. The frontend must have a truthful recovery action; a retry hint cannot represent an irrecoverable terminal request. Preserve ownership checks before looking up or returning an existing request.

- [x] Refresh bounded technical event correlation and the linked payment state without opening clinical answers or replaying checkout in production.
- [x] Reproduce the branch using isolated guest and authenticated fixtures. In particular, try a restored flow ID after cancellation with a new submission key; distinguish a uniqueness collision from profile or answers failures.
- [x] Capture the actual failing assertion before changing code. If this lead does not reproduce, trace the other persistence branches; do not ship a speculative retry.
- [x] Reconcile the original Stripe obligation before offering a fresh checkout. Cancelled/unpaid database state alone is insufficient: provider-paid returns the safe existing outcome; processing/in-flight/unknown blocks another payment and enters recovery; a still-open session needs the existing bounded audited invalidation and confirmed read-back; only terminal-unpaid provider evidence permits a new payment attempt. Reassert the stored session/state against concurrent webhook or cancellation changes.
- [x] Implement the smallest proven repair. Once the old obligation is verified unpayable, a new request must use a deliberate fresh flow/submission identity and revalidate the current answers and consent; it must not reopen the old payment obligation. Preserve entered information only where current safety and draft-expiry rules allow it.
- [x] Give expected recovery states specific bounded diagnostics. Unexpected persistence failures must reach Sentry with the failing operation and safe database code, without identifiers, raw error payloads or clinical data.
- [x] Verify the matrix below, then commit the checkout change independently.

| Fixture | Required result |
|---|---|
| Cancelled unpaid request, restored draft | Clear fresh-request recovery; original remains cancelled and no old session becomes payable |
| Database cancelled/unpaid but Stripe paid, processing, unknown or still open | No fresh payable obligation until provider reconciliation/invalidation is confirmed; payment/cancellation races cannot produce two payable sessions |
| Same owned pending request, repeated submit | One payable current session; no duplicate intake/payment |
| Paid/refunded/disputed request | Existing safe result; never another payment |
| Expired or replaced session | Current-session guards hold; stale webhook cannot mark paid |
| Different owner or uncertain identity | No request disclosure or recovery access |
| Missing/changed required clinical answer | Existing validation remains enforced |
| Network loss, Back, reload, another tab | Retry is bounded and state stays coherent |

## Task 2 — Harden and verify the profile backfill

**Files:** `scripts/encrypt-phi-backfill.ts`, its existing primitive `lib/security/encryption.ts`, `docs/SECURITY.md`. Locate and extend the existing script contract tests; add an isolated PostgreSQL race test if required to prove the conditional update.

**Interface:** Retain the existing encryption format and `ENCRYPTION_KEY`. Do not involve envelope-key rotation. Applying a missing field must compare the source snapshot and require that the target ciphertext is still absent; concurrent edits/new ciphertext must cause a skip and reread, never overwrite.

- [x] Read the current CLI flags and identify any status-table writes before calling dry-run; dry-run must itself be read-only.
- [x] Add a production-key compatibility preflight using existing ciphertext privately, with aggregate mismatch/decryptability counts. Fail closed if compatibility cannot be established; never print the sampled values or keys.
- [x] Make updates compare-and-set and make success counts reflect rows actually changed. Use keyset pagination or another scheme that cannot skip rows as the missing-field set shrinks.
- [x] Test wrong key, changed plaintext, newly added ciphertext, null/empty fields, partial batches, resumability, fixture exclusions and idempotent reruns.
- [x] Replace identifier/raw-error output with aggregate results and bounded error categories. Verify that both stdout and persisted migration-status errors are safe.
- [x] Produce and independently review the exact dry-run packet: project identity, code SHA, field counts, exclusions, key compatibility, guarded-write method, verification command and stop conditions.
- [x] Resolve apply authorization against the current session before executing the reviewed packet.
- [x] Freeze the preflight candidate/field/snapshot set and require a matching approved scope fingerprint and counts before any write. Prove insertion, newly eligible rows, equal-count substitutions and a nonzero zero-change rerun cannot expand the approved apply.
- [x] After an authorized apply, verify parity, decryptability, remaining exceptions and a zero-change rerun. Record the aggregate receipt in SECURITY.md. Retain plaintext until the separate reader audit and retirement decision; do not remove keys or historical rows.

## Task 3 — Restore trustworthy monitoring

**Files:** `.github/workflows/prod-request-flow-synthetic.yml`, `e2e/prod-request-flow-synthetic.spec.ts`, `lib/monitoring/cron-heartbeat.ts`, `lib/monitoring/critical-alert-cooldown.ts`, `app/api/cron/business-alerts/route.ts`, the existing health-check route, `docs/OPERATIONS.md`.

**Interface:** Distinguish scheduler invocation, completed browser checks, last success and last failure. Repeated health measurements remain available even when unchanged incidents stop paging.

- [x] Refresh at least 24 hours of synthetic run timestamps and actual assertions. The current entry-flow synthetic does not prove a completed hosted payment.
- [x] Add independent freshness detection for missing browser runs; the watched workflow cannot be the only mechanism detecting its own absence. Confirm the existing provider/credential options before choosing execution infrastructure.
- [x] Select an explicit attainable cadence and detection delay. Use existing infrastructure where it meets the requirement; if a new paid runner is necessary, present its exact cost/setup packet before provisioning. Do not call the monitor five-minute coverage until completed-run evidence proves it.
- [x] Test scheduler silence, a failed run followed by success, delayed completion and a currently running check. Demonstrate both the outage alert and recovery.
- [x] Give Sentry stable per-incident fingerprints and state-aware repeat handling. A new category, worse severity, count increase or failure of the deduplication store must remain visible. Preserve periodic aggregate health metrics and independent Telegram rules.
- [x] Investigate any fresh feature-flag transport failure after the retry release. Retain the retry and cache guarantees; a single background revalidation error is not sufficient evidence for a broad dashboard retry layer.
- [x] Commit monitoring separately; record actual cadence, coverage and retained limitations in OPERATIONS.md. Production observer execution/recovery is verified; the first full day of the new browser schedule remains an explicit measurement handoff.

## Exit and handoff

Run the applicable focused suites, lint/typecheck, isolated database checks and required release pipeline under the shared session protocol. Record each exact tested head, merged SHA, ready production deployment, smoke/browser evidence and rollback scope. Source/CI proof does not substitute for production data parity or observed scheduler cadence.

The nine retrospective clinical cases remain clinician-owned work; they are not an AI outcome or a build-completion target. Leave the March certificate unchanged and unsent. The two April Ads discrepancies remain recorded historical uncertainty.

If production repair awaits authorization or an external prerequisite, label it explicitly outstanding and ship independently complete code slices; do not claim this whole plan complete. The operator decides whether the next session may proceed with that bounded item outstanding.

**Next-session prompt:** “Execute the certificate revenue plan linked from ROADMAP. Read Session 1's release receipts first and refresh any dated operational/commercial evidence. Work only that plan.”

## Execution receipt

**Bounded implementation, required PR checks and production releases are complete and receipted below.** The historical repair was applied and independently verified on 2026-09-07. The later approved checkout and monitoring review corrections are released in PRs #532–#534. Scheduled freshness has recovered; dependable cadence, the next normal review-request outcome and provider-delivery evidence retain their explicit observation handoff. Plan 2 has not started.

### Starting evidence

- Planning PR [#524](https://github.com/reabal-n/instantmed/pull/524) merged as `7b81134264cd01cd194e1e7310c2915341feaee1` at 2026-09-06 06:07:46 UTC. The five plans and canonical sequence were present before implementation. Its required checks passed.
- Initial production was READY at `56adfcc8be76508aa0299ea85611e83b094b1dc9`, including the existing feature-flag retry. Reproductions used isolated fixtures, with no live checkout replay or production environment loaded into tests.
- The 12:18 Sydney restored-draft incident correlated to an existing cancelled, failed/unpaid intake. A private read of its exact Stripe Session confirmed expired/unpaid with a cancelled PaymentIntent and matching ownership metadata. No provider mutation was performed. The later refresh found a different paid request for the same owner at 15:53 Sydney; the earlier “no later payment through 14:42” statement is dated evidence, not the current customer status.

### Checkout release

[PR #525](https://github.com/reabal-n/instantmed/pull/525) is merged and deployed. Actual guest and authenticated entrypoints reproduced the unique-flow collision with a restored flow and a new submission key: two assertions failed before repair. The database fixture separately reproduced SQLSTATE `23505` from the unchanged canonical uniqueness index.

The repair requires ownership or exact converted guest-bearer possession before recovery. It reconciles the stored Stripe Session and expanded PaymentIntent, verifies any bounded invalidation with a separate read, then reasserts current owner/session/status with a conditional write. Only confirmed terminal-unpaid obligations permit an explicit fresh request with new identity and current safety/consent validation. Unknown/processing states block; paid/refunded/disputed states return the safe existing outcome. Missing guest proof exposes sign-in/support and disables the old payment action. Cancelled requests remain cancelled. Unexpected database failures retain bounded operation/SQLSTATE diagnostics without identifiers or raw payloads.

- **Local:** 489 regression tests across 36 files, including the 151 focused entrypoint/helper cases; eight separate PostgreSQL/PostgREST cases; lint, typecheck, doc audit and dead-code gate passed. Eight actual ReviewStep/store/CSS views covered desktop/mobile, light/dark, reduced motion, keyboard, failure/reset and sign-in recovery. This was a component harness, not a hosted Stripe payment. The strict live integration checker was run separately and passed read-only; an entire local `release:check` run is not claimed.
- **Review:** scoped and independent final release reviews passed after correcting a guest possession gap found during final review. The unchanged final P1 regression was independently rerun and passed.
- **CI:** tested head `4f71ff656dd39aaf0a16fdf4a10ec6a649610f45`; [run 34018839733](https://github.com/reabal-n/instantmed/actions/runs/34018839733) passed build, required E2E and Lighthouse. The nonblocking `/request` chunk-size advisory was identical to planning PR #524, not a new regression.
- **Release:** merge `ea57b0346358351cc514d0168204bcda1a5c6e55`; primary production alias verified against READY deployment `dpl_9DzUuDi25vnBrzxB6P8YBcECnTHk` at 2026-09-06 08:05:42.630 UTC. [Post-deploy smoke 34020902736](https://github.com/reabal-n/instantmed/actions/runs/34020902736) passed. Post-merge [CI 34020781827](https://github.com/reabal-n/instantmed/actions/runs/34020781827) also passed.
- **Production browser:** one-off invocation of the existing isolated seven-case synthetic [34021064088](https://github.com/reabal-n/instantmed/actions/runs/34021064088) passed; its actual browser step ran 08:10:17–08:10:50 UTC. This verifies live request entry and browser assertions, not hosted payment or scheduled cadence. Deployment-scoped error/fatal and 5xx aggregates were empty through 08:12 UTC, a bounded observation only.
- **Rollback:** restore the previous READY application deployment and revert PR #525 through normal governance. There is no schema rollback or patient/payment-state repair to undo. Preserve exact-current-session guards and investigate provider state before any payment recovery.

### Profile encryption — production complete

The original CLI's second discovery scan could write candidates absent from the approved preflight. [PR #528](https://github.com/reabal-n/instantmed/pull/528) fixed both reproduced races by freezing candidates, binding their complete comparison snapshots to an opaque fingerprint and requiring a separate zero-change expectation. It passed 146 HTTP/PostgreSQL cases and independent review. Exact head `6b8d8eb6fd6f18dba41efba50aa3e335101a9023` passed [CI 34029654838](https://github.com/reabal-n/instantmed/actions/runs/34029654838), merged as `b8951b8366814e60d895f264ec533798179ce5a5`, and reached READY deployment `dpl_2fd3sLY6oYcL32ubERGuy4fgiT3X` at 2026-09-06 12:29:35.422 UTC. [Post-deploy smoke 34033255415](https://github.com/reabal-n/instantmed/actions/runs/34033255415) and [main CI 34033137460](https://github.com/reabal-n/instantmed/actions/runs/34033137460) passed.

The operator explicitly approved execution on **2026-09-07**, excluding patient contact. A fresh 04:06 UTC dry run used the current Vercel production key and authenticated all 334 existing eligible copies. It found 599 scanned / 51 excluded / 548 eligible / 466 candidate profiles, with 452 DOB, 300 phone and 21 Medicare copies missing. The changed fingerprint required a separately preserved and independently reviewed expectation; the September 6 packet was not overwritten.

**Applied and verified:** the guarded apply completed at **04:09:28.312 UTC**, filling **773 copies across 466 profiles**, with zero skips and errors. The 04:11 UTC read-only verification found zero candidates/missing copies and **1,107/1,107 authenticated copies**. Four pre-existing parity differences remained preserved. A separately reviewed conditional zero-change apply then processed and changed zero profiles/fields and left the sole completed migration-status row unchanged.

The [PHI-free execution receipt](../receipts/2026-09-07-profile-encryption-repair.json) records exact scope, source, timings, field counts and both runs. [SECURITY.md](../../SECURITY.md#historical-profile-repair--completed-2026-09-07) owns the operating details and preservation/rollback boundary. No plaintext retirement, key rotation, clinical change or patient contact occurred.

### Monitoring

Implementation and independent reviews are complete in merged [PR #527](https://github.com/reabal-n/instantmed/pull/527). That initial release requested four-hour GitHub browser execution at minute 17; the September 7 follow-up below changes the request to two hours. An independent observer in the existing five-minute Vercel health check evaluates actual browser-step evidence and six-hour freshness. The design separates invocation, running state, maximum completion time, source-ordered outcome, last success/failure and observer availability. Durable enablement and state-aware incident observations use one narrowly scoped service-role-only append RPC over existing operational metrics. No new paid provider, environment variable or Codex scheduled task is introduced.

Local proof includes 111 focused tests before review corrections and 77 affected tests on the final corrected source, plus actual PostgreSQL ACL/concurrency/ordering/retention/grace checks. Lint, typecheck, documentation and actual dead-code ratchet passed (2305/2305; no weakening). Reviews caught and corrected delayed historical failure notifications, unavailable/priority-masked purchase recovery, and unused production exports. Final source was approved at `52bc7e5582076e8369a3381df1a3c2e1417f0396`; main integration produced `db9cec525c428e6f5cec7db19ee37478f69b971e` with identical stable patch ID `b4779b60dbf76f23607cc716b452b22172bc1bec`. Final receipt-only head `f225c1d807997f91c28b6bb53f73d17934a57565` retains that source; required [CI 34027297774](https://github.com/reabal-n/instantmed/actions/runs/34027297774) passed build, Lighthouse and the complete required E2E suite. PR #527 merged at 11:09:33 UTC as `f7b2e43aa3d126cb05020c0ea7b27e016feeb82d`. The primary alias matched READY deployment `dpl_Foxpmwb6xNG5NeEQfbKWuSPYvkZw` at 11:12:08.402 UTC; [post-deploy smoke 34029556180](https://github.com/reabal-n/instantmed/actions/runs/34029556180) passed. Main post-merge [CI 34029432020](https://github.com/reabal-n/instantmed/actions/runs/34029432020) initially failed before the checkout database test started: the public container registry returned a rate limit while pulling the unchanged PostgREST image (exit 125). The follow-up PR CI passed that database gate, and the failed-job rerun then passed build, Lighthouse and all required E2E stages at 12:26 UTC. No test failure was relabelled as success.

The refreshed 24-hour baseline ending 06:30:37 UTC had nine successful scheduled runs. Completed gaps were 98.18 minutes minimum, 118.15 median and 264.45 maximum despite the old five-minute expression. Each ran seven assertions; none proved hosted payment. Checkly access was not established (401), and Sentry issue access returned 403. Public GitHub reads and the existing Vercel cron support the chosen observer, with bounded API work and explicit rate-limit/unavailability handling.

One background feature-flag revalidation transport error was observed after the existing retry release, with no demonstrated new foreground failure. The pinned Next.js cache behavior preserves the cached response. Existing retry/cache tests pass; no speculative dashboard retry layer or broad error suppression was added. A later refresh found separate dashboard intake/certificate-readiness fetch error clusters at 08:16:31–32 UTC with ECONNRESET; existing source forwards an explicit data error to the patient UI. Foreground impact is not established from the RSC evidence, and no recurrence was observed from 08:16:33 through 08:51. A subsequent grouped read from 08:51–09:52 UTC found no error/fatal or 5xx entries on that checkout deployment. The next profile-tooling deployment also had empty grouped error/fatal and 5xx results over 09:58:17–10:53:00 UTC. These bounded windows are not a zero-error lifetime claim.

Read-only migration preflight found 78 existing operational metrics rows and no new state/index/RPC. An isolated linked-CLI dry run listed exactly `20260906100000_monitor_observation_state.sql`; a fresh dry run before release preparation confirmed the same sole pending migration. After exact-head CI passed, the linked CLI applied only that reviewed file. Production read-back at 11:08:06 UTC confirmed the exact migration, both initial state rows, valid/ready unique index, fixed empty search path and service-role-only RPC access; table SELECT/INSERT-only grants were unchanged. The ACL checker returned zero violations, schema error lint passed, and canonical migration history aligned through `20260906100000`. Existing observer-host activity was verified separately: health heartbeat 08:45:07 UTC and 288 HTTP 200 health responses over the preceding 24 hours. Five scheduled observer polls at 11:15:08.115, 11:20:08.192, 11:25:07.825, 11:30:07.568 and 11:35:07.638 UTC persisted versions 2–6 and verified 2/4/6/8/10 historical attempts. The observed poll gaps were 299.633–300.077 seconds. Enablement remained 11:07:55.608 UTC. The final snapshot reported observer available, no coverage gap and no active browser incident; the startup-unavailable incident recovered after bounded history processing. Latest verified browser success was scheduled run `34024261028`, whose named step completed at 09:19:28 UTC; manual run `34021064088` remained separately identified. A later read confirmed version 14 at 12:15:07.493 UTC, with the same enablement, available observer and no active browser incident. This proves production observation/recovery, not a completed new four-hour scheduling window.

The first business observation at 11:30:08.585 UTC persisted version 2 and retained one critical `ops_approved_certificate_missing_record` incident. A fresh aggregate check found one March 2026 missing-current-valid record with only superseded certificate history, and zero records from other periods. The operator-directed unchanged/unsent boundary remains in force. At 11:35 the combined health heartbeat remained `partial_failure` because `google-ads-conversions` and `review-request` retain unrecovered failures from before this deployment (latest failures 10:45:23 UTC and 00:00:15 UTC respectively). Browser recovery does not clear those separate jobs. Their root causes are not established by heartbeat evidence alone; no Ads mutation, outreach retry or status reset was performed. The 12:00:09.211 UTC business poll retained that same single incident in version 3. Deployment-scoped error/fatal and 5xx aggregates were empty from 11:12:09–12:19:00 UTC; this is a bounded window, not an all-clear.

Required monitoring CI, schema application, deployment/smoke and production observer execution/recovery are complete. The first full day of the requested browser schedule and Sentry provider delivery remain explicitly separate observations.

### September 7 monitoring and release follow-ups

Read-only production review through **04:10 UTC / 14:10 Sydney** found the observer available, with no coverage gap. Its 203 evaluations through 04:05 had **298.981–301.312-second** gaps. A stale incident activated September 6 at 15:20 and recovered at 15:30. A second activated in version **205 at 04:10:07.494 UTC** after the latest browser completion crossed six hours at 04:08:37. No incident was cleared manually.

Three post-release scheduled browser steps passed: [34042318145](https://github.com/reabal-n/instantmed/actions/runs/34042318145) completed at 15:27:56 UTC, [34051180542](https://github.com/reabal-n/instantmed/actions/runs/34051180542) at 18:17:48, and [34063034637](https://github.com/reabal-n/instantmed/actions/runs/34063034637) at 22:08:37 on September 6. Those completion gaps were 169.87 and 230.82 minutes; the earlier pre-release 09:19:28 completion to first post-release completion gap was 368.47 minutes. The two observed breaches justify requesting the existing seven-case check every two hours instead of four. The independent five-minute observer, 360-minute freshness threshold and eight-minute browser timeout remain unchanged. GitHub can delay/drop scheduled jobs; source cadence is not observed coverage.

The 00:00 UTC review-request batch reported **13 sent / 2 transiently blocked / 0 provider or reconciliation failures**. Bounded aggregate evidence identified both blocked rows as successfully persisted, future-scheduled `patient_cooldown` deferrals. The route had counted these expected deferrals as failures. The correction adds `requestExpectedDeferrals` as a subset of the unchanged transient-block count and excludes only this known persisted outcome from heartbeat failures. Failed persistence, policy reads, unknown reasons, provider failures and reconciliation failures remain unhealthy. No send behavior or cooldown rule changes, and no production send route was invoked to verify this correction.

The 03:45 Ads batch had 447 candidates, zero processed uploads and zero provider/adjustment failures. Two expired conversion targets still correctly cause partial failure; current unknown/reserved/conflict/stale-pending counts were zero. Historical counters and the March superseded-certificate incident remain preserved. No fresh feature-flag warning/error/fatal matches were found in the bounded September 6 11:12:09–September 7 04:05 window. Separate intake-read transport clusters at 02:42:38 and 02:46:51 retain explicit patient-facing errors; they do not justify a broad retry layer or establish foreground impact.

PR #530's original build and single failed-job rerun stopped before checkout database tests because the public ECR registry returned HTTP 429. The database fixture passed locally. The bounded CI correction retries retrieval and permits the official Docker Hub mirror only at the **identical v14.12 manifest digest**, verified byte-for-byte across both registries. It never changes the database version, retries a failed test or skips a gate. Both actual PostgreSQL suites passed using the mirror (checkout 8, profile 146 cases).

**Local/review:** the monitoring slice passed 102 focused/adjacent tests, lint, typecheck, doc audit and the unchanged dead-code gate. The CI slice passed 10 cache/retry/fallback/failure-boundary tests, both PostgreSQL suites, runtime/pin checks, lint/typecheck, doc audit and dead-code checks. The integrated change passed 50 affected tests, lint, typecheck, 124 documentation contracts and the unchanged 2305/2305 dead-code ratchet. Independent source and final-documentation reviews returned KEEP.

**Required CI and release:** [PR #531](https://github.com/reabal-n/instantmed/pull/531) passed [CI 34083595707](https://github.com/reabal-n/instantmed/actions/runs/34083595707) on exact head `3360273d98d3fc15b6771b1a3114b10f981c38fd`: build, Lighthouse and the complete required browser gate. The unit run recorded 7,358 passed / 82 skipped across 755 passed / one skipped files; the separate actual database suites passed 8 checkout and 146 profile cases. Browser stages passed 8 ops (one skipped), 79 certificate-readiness, 86 prescribing, one repeat-prescription integration and five signed-resume cases. Existing skips are not represented as executed proof. The hosted runner retrieved the pinned ECR digest successfully; mirror fallback has separate actual local database proof.

PR #531 merged at **2026-09-07 05:18:53 UTC** as `3ff545c61587beaff581fc4854f42f0e1447ea24`. The production alias matched READY deployment `dpl_8HEE8tav898735vnyWZafvsGYyju` at **05:21:27.386 UTC**. [Post-deploy smoke 34086502301](https://github.com/reabal-n/instantmed/actions/runs/34086502301) passed, and the 05:22 UTC public health check returned HTTP 200 / `status: ok`. The separate [main post-merge recheck 34086333585](https://github.com/reabal-n/instantmed/actions/runs/34086333585) passed build, Lighthouse and full E2E.

**Observed recovery before this follow-up deployed:** the fourth old-schedule run [34083759366](https://github.com/reabal-n/instantmed/actions/runs/34083759366) was invoked at 04:36:56 UTC and its browser step passed at 04:37:56–04:38:28. The gap from the preceding 22:08:37 completion was 389.85 minutes. By version 213 at 04:50:07.647, browser incidents were clear with an available observer and no coverage gap. A separate script-notification SLA incident recurred at business version 36 (04:30:08.663) and recovered in version 37 (05:00:08.508); its section-read-failure metric stayed clear. The March certificate incident remained active. These are actual persisted transitions, not Sentry delivery receipts or new two-hour-cadence proof.

**After-deploy observer read:** version 220 persisted at **05:25:08.257 UTC**, with the same enablement, an available observer, no coverage gap and no active browser incident. The 18 actual evaluations since 04:00 had 299.065–300.869-second gaps. The 05:25 health heartbeat remained `partial_failure`; the review-request and Ads historical failed outcomes were not reset. The public health response proves its bounded database check, not that every operational incident is clear.

**Rollback:** revert PR #531's reporting/workflow/retrieval changes if required. Preserve durable observation history, plaintext, keys, authenticated ciphertext and the completed profile receipt. No schema or environment change accompanied this follow-up; no patient-send route was invoked.

### Approved Plan 1 review follow-up — released

- **Checkout recovery:** a restored request with both request status and payment status `expired` reaches a confirmed dead end even though it belongs in the same terminal-obligation reconciliation as `cancelled`. The reviewed correction preserves exact owner or converted-bearer proof, service ownership, current session/status/payment/error compare-and-set, and terminal-unpaid provider proof. It presents honest payment uncertainty: return to the original request only after ownership and service are verified; otherwise disable Pay and route to sign-in or support. Back/reload keeps the bounded flow/bearer and repeats server reconciliation; no client approval or recovery destination is persisted. Safe database/provider diagnostics use fixed operation codes and bounded SQLSTATE only.
- **Monitoring state:** a partial second provider read can discard already verified first-read evidence when the cache is full, and a compare-and-set retry can discard a competing observer's immutable cache/backoff. The released correction merges the bounded immutable evidence, preserves failure and rate-limit state, and pins the workflow file, effective job, executing browser step, incident sections and all 40 existing metric IDs with source contracts. Focused proof demonstrates failure, later success/recovery and a subsequent quiet poll in sequence. Any bounded `unavailableReason` is authenticated-response diagnostics only, not persisted state.
- **Cadence semantics:** `completedAt` remains the maximum actual verified completion for browser execution/availability. The released correction requires retained verified `event: 0` **and `attempt: 1`** completion for scheduled freshness; durable enablement supplies grace when no completion has ever existed. Manual dispatch and reruns prove browser execution, never a new scheduled invocation. If retained completions contain no known scheduled first-attempt proof, cadence is unknown and the observer is unavailable; manual evidence cannot mint a scheduled green state or automatically clear an older stale incident. The existing `branch=main` runs filter plus accepted `head_branch: main`/exact-path schema means the feature-branch poisoning claim was not reproduced. Public bounded reads remain the current source. A least-privilege authenticated GitHub Actions read credential is optional future resilience work; none is configured in current server code or reviewed production environment metadata, and no quota outage has been demonstrated.
- **Evidence and acceptance:** the identical-digest registry fallback and durable expected-cooldown reporting remain justified by observed ECR 429 and `patient_cooldown` incidents. Local proof, independent reviews and required integrated CI are complete for both checkout and monitoring corrections. Production receipts below distinguish deployment/smoke and safe entry checks. The required checkout browser seam runs after Chromium installation. The local ReviewStep/store seam, provider browser E2E, hosted Stripe payment, Sentry provider delivery and scheduled cadence remain separate evidence.
- **Preserved handoff:** the original observer enablement remains 2026-09-06 11:07:55.608 UTC. Retain the full two-hour cadence measurement and next normal review-request observation without invoking a patient-send route, resetting historical heartbeat state or creating a scheduled task. Google Ads' two expired conversion targets remain actionable; the March certificate stays unchanged and unsent. The completed aggregate profile repair needs no rerun or new approval. The nine retrospective cases and two April Ads discrepancies retain their separate boundaries.

**Read-only observation checkpoints:** the **2026-09-07 12:00 UTC (22:00 Sydney)** browser-cadence read completed unhealthy; actual scheduled completion and native recovery followed at 12:45 and 12:50, as recorded below. Retain the complete two-hour schedule measurement window **2026-09-07 05:18:53–2026-09-08 05:18:53 UTC** (ending 15:18:53 Sydney), and the next normal review-request outcome after **2026-09-08 00:00 UTC (10:00 Sydney)**. Read persisted/provider outcomes; do not invoke the send route or substitute manual dispatch or reruns for scheduled-cadence proof. Elapsed time alone closes none of these observations.

**Actual silence detection — 2026-09-07:** the last scheduled browser step remained run `34083759366`, completed at **04:38:28 UTC**. The six-hour threshold was crossed at **10:38:28**. Production observer version **283 at 10:40:08.844480 UTC** persisted `browser_stale`, following a clear version 282 at 10:35:08.477891. The observer was available, with no coverage gap or backoff and a ten-entry cache. Detection occurred about 1 minute 41 seconds after the threshold, inside the five-minute polling target. Direct GitHub reads still showed no later run at 11:14 UTC. This is actual silence detection on the preceding `e3382f2` deployment, not proof of the new two-hour cadence or Sentry delivery. At that cutoff scheduled execution remained stale; the later actual recovery is recorded below, while cadence dependability remains an open observation.

**Fresh feature-flag investigation — 2026-09-07:** the deployment-scoped **06:27:56–10:28:00 UTC** window on `e3382f2` contained one feature-flag error at **09:37:09**, attributed to `/_not-found`: a `TimeoutError` during Next cache revalidation. There was no matching uncached-default or explicit-refresh failure message and no HTTP 5xx in that window. The pinned Next source retains the existing cached value after this revalidation error; this is source behavior, not a measurement of the caller's rendered flags. All nine existing feature-flag cache/retry regressions passed. The underlying transport cause and client impact remain unproved. Preserve the bounded retry, cache guarantees and visible error; this event does not justify a general dashboard retry or error suppression.

**Local monitoring proof — 2026-09-07:** initial source `b2dc17fdd6d55cfaa9db3d3724cd9ebe998aabdc` passed 153 affected/adjacent tests across 17 files. Independent review then reproduced a false recovery when an older fetched list overwrote a competing poll's newer incomplete window. Corrected source `c42046ff738e60eb28819ea72ce6204935dc6209` passes **160 tests across 17 files**, including 43 browser-observer tests and both independent reproductions. Seven durable provenance cases cover CAS and ordinary later stale-list reads, pending jobs, coverage gaps, running state, complete later evidence and a quiet recovered poll. Cache overflow, CAS evidence/backoff loss, manual-cadence false health and six incorporated provenance cases failed before their corresponding fixes. The disposable PostgreSQL fixture verifies the unchanged state schema's ten-entry bound, partial evidence/backoff persistence, ACLs, ordering, CAS and retention behavior. Lint, typecheck and the unchanged 2305/2305 dead-code ratchet pass. Canonical documentation passes 124 contracts with 134 Markdown files. Independent re-review returned KEEP; required CI and deployment are receipted below. These local results do not establish delivery or scheduler cadence. The obsolete draft [CI 34111245782](https://github.com/reabal-n/instantmed/actions/runs/34111245782) passed build, then was cancelled for the review correction; it is not release proof.

**Checkout follow-up proof — 2026-09-07:** final source `4a70cb9376812fa54d1992f78c15ab2d54e59ebc` passed 420 affected tests across 15 files and **49 disposable PostgreSQL/PostgREST cases**, plus lint and typecheck. Independent review returned KEEP after **479 regressions across 19 files**, all 49 database cases, both original key-hit probes and 12 actual unified-action/helper/reconciler challenges. The two ownership defects were reproduced before their corrections: original submission-key hits could skip converted-bearer proof, and the unified guest shortcut could accept null captured-email/stored-flow evidence. Required internal proof mode now comes solely from server authentication; forged client flags cannot change it. Authenticated legacy fields and ordinary unconverted first checkout remain covered. Rebase onto monitoring merge `011d89ff92b89f973b7fd76c956cdf12d910918f` preserved the reviewed source diff byte for byte; only documentation inventory counts needed conflict resolution. The integrated docs pass 124 contracts and the 134-file audit.

The isolated browser fixture passed **32 ReviewStep/store views** at 1280×900 and 390×844 in light/dark with reduced motion, with zero external requests. Back/remount/reload retained the original flow and bearer, and explicit fresh restart cleared identity, answers and consent; normal store validation prevented an incomplete fresh form jumping to Review. The required CI now runs that seam immediately after Chromium installation. This is component/store interaction evidence, not the full Next request shell, hosted Stripe payment or production checkout replay. Required integrated CI passed; production proof is recorded separately below.

**Monitoring follow-up release:** [PR #532](https://github.com/reabal-n/instantmed/pull/532) passed [CI 34112498366](https://github.com/reabal-n/instantmed/actions/runs/34112498366) on exact head `d5e81a12089a1c4147e16e64346a7b757d91f5f7`: build, Lighthouse and full required E2E. The unit suite passed 7,382 with 82 existing skips; the separate database suites passed 8 checkout and 146 profile cases. Browser stages passed 8 ops (one skipped), 79 certificate readiness, 86 prescribing, one repeat-prescription integration and five signed-resume cases. Independent source re-review returned KEEP at `c42046ff738e60eb28819ea72ce6204935dc6209`; documentation review returned KEEP with the restored observation dates. PR #532 merged at **11:17:58 UTC** as `011d89ff92b89f973b7fd76c956cdf12d910918f`. The production alias matched READY deployment `dpl_8hZJ7YxF6mhnV96go7oQdqfkf4eS` at **11:20:59.620 UTC**. [Post-deploy smoke 34116164680](https://github.com/reabal-n/instantmed/actions/runs/34116164680) passed. The separate [main recheck 34115912688](https://github.com/reabal-n/instantmed/actions/runs/34115912688) also passed build, Lighthouse and full E2E. No schema, environment, schedule or dependency change accompanied this release. Rollback is a revert of PR #532's application/reporting changes, preserving durable observation history and the completed profile-repair receipt.

**Monitoring runtime acceptance:** the first native poll after PR #532's deployment was version 292 at **11:25:09.215228 UTC**, with original enablement retained, an available observer, ten cached receipts, no coverage gap/backoff and the existing stale-browser incident preserved. The second poll at 11:30 agreed. Business version 50 at 11:30 retained the single March certificate incident. Deployment-scoped 11:21–11:33 UTC aggregates had 20 info, three warning and zero error/fatal entries, no HTTP 5xx and no feature-flag matches. One warning was the retained business alert; the other two were not attributed. This bounded read is neither a zero-warning claim nor provider-delivery proof.

**Checkout follow-up release:** [PR #533](https://github.com/reabal-n/instantmed/pull/533) passed [CI 34116433616](https://github.com/reabal-n/instantmed/actions/runs/34116433616) on exact integrated head `b60a32a1069a9272cdd2387c185ddbdd64a18f45`: build, Lighthouse and the full required E2E gate, including the new 32-view restored-checkout seam. Units passed 7,480 with 123 skips; separate database gates passed **49 checkout and 146 profile cases**. The additional 41 ordinary-unit skips were disposable database cases exercised by that separate gate. Browser stages passed eight ops (one skipped), 79 certificate readiness, 86 prescribing, one repeat-prescription integration and five signed-resume cases. PR #533 merged at **12:34:25 UTC** as `d9c0921f1b3121fdda346875b92ed26bb9bc1c84`; the merged tree exactly matches the tested head. The primary alias matched READY deployment `dpl_4uubGunZKFCw8YDyx1DKdvNPKfC8` at **12:37:08.239 UTC**, and [post-deploy smoke 34122854667](https://github.com/reabal-n/instantmed/actions/runs/34122854667) passed. The separate [main recheck 34122601826](https://github.com/reabal-n/instantmed/actions/runs/34122601826) passed build, Lighthouse and full E2E. No schema, environment, dependency or pinned-stack change accompanied checkout. Rollback is a revert of PR #533's application changes while preserving terminal rows, current payment references, uniqueness constraints and the completed repair/monitor history.

**Checkout production browser:** the safe anonymous seven-case request-entry suite passed against `https://instantmed.com.au` after the matching deployment, completing in 24.7 seconds with the result read at **12:39 UTC**. It checked desktop/mobile entry controls, readability, layout, cookie placement and active pathway entry. The existing browser isolation intercepted `/api/draft` and `/ingest`; no database seeding, production checkout replay, patient contact or hosted payment was performed. This manually run local browser check is live entry evidence, not a scheduled GitHub completion or a production exercise of restored-payment recovery.

**Checkout native observation:** the first normal observer poll after deployment, version **307 at 12:40:09.240012 UTC**, preserved original enablement, ten cached receipts, available observation, no coverage gap/backoff and the existing stale-browser incident. The public health read at **12:40:41.433614 UTC** returned HTTP 200 / `status: ok`; that endpoint proves only its bounded database check. Neither read establishes new scheduled-browser coverage or Sentry delivery.

**Additional scheduled-rerun correction — released:** GitHub preserves the original `schedule` trigger when rerunning a scheduled workflow and increments its attempt. Independent read-only provider metadata established this on [dotnet/runtime run 30749676863](https://api.github.com/repos/dotnet/runtime/actions/runs/30749676863): attempt 2 ran the next day while retaining the original trigger and run identity. No InstantMed workflow was dispatched or rerun for this investigation. Six of seven independent source probes reproduced false freshness on the preceding monitor source. The correction requires `event === 0 && attempt === 1` for cadence without rewriting stored metadata, cache receipts, all-run completion time or execution recovery. Four durable regressions failed before the fix and passed after it, covering fresh/cached reruns, unknown cadence, genuine scheduled recovery and legacy compare-and-set evidence. Local checks passed **86 affected tests across eight files**, lint, typecheck and 124 documentation contracts. Independent exact-source re-review returned KEEP at `acd2728420fbe340d0131fa45f4697c751b7f88d`, with seven challenge probes and 61 repository tests passing. Rebase onto checkout merge `d9c0921f1b3121fdda346875b92ed26bb9bc1c84` produced `a5ccf3c13dd17236faa78b066ef35cdb52cbdb53` with the reviewed three-file patch byte-identical. Required integrated CI, deployment and native compatibility acceptance passed, as receipted below. No schema, environment, provider, credential, schedule or dependency change is required; rollback preserves all observation history.

**Scheduled-rerun release:** [PR #534](https://github.com/reabal-n/instantmed/pull/534) passed [CI 34123635676](https://github.com/reabal-n/instantmed/actions/runs/34123635676) on exact integrated head `351e6187a07f3c969904e8804e11e1cf88bad498`: build, Lighthouse and full required E2E. Independent integration review confirmed all three reviewed source/documentation files and their patch remained byte-identical to accepted `acd2728420fbe340d0131fa45f4697c751b7f88d`. Units passed 7,484 with 123 skips; separate database gates passed 49 checkout and 146 profile cases. Browser stages passed eight ops (one existing skip), 79 certificate readiness, **85 prescribing plus one flaky test that passed on its automatic retry**, one repeat-prescription integration and five signed-resume cases; the 32-view checkout seam also passed. The flaky profile-drawer assertion and the separate main recheck are recorded below, without relabelling the first attempt as a pass. No workflow was manually rerun and no gate was weakened.

PR #534 merged at **2026-09-07 13:51:19 UTC** as `14bfee7529b1aa3222b55e62008b923fe71d64e5`; its full tree matches the tested head. The primary alias matched READY deployment `dpl_CMae1XfUxdGjJJVMBxC2WU9fJX3d` at **13:53:36.972 UTC**, and [post-deploy smoke 34129937317](https://github.com/reabal-n/instantmed/actions/runs/34129937317) passed. Additional post-merge verification is linked separately in [main recheck 34129727687](https://github.com/reabal-n/instantmed/actions/runs/34129727687); the required PR result above is its own release evidence. No schema, environment, provider, credential, schedule, dependency or pinned-stack change accompanied this release. Rollback is a revert of PR #534's cadence predicate, associated tests and semantics documentation; retain all persisted evidence, enablement and observation history.

**CI flaky assertion:** `e2e/doctor.prescription-ui.spec.ts:375` timed out after 15 seconds waiting for the saved-profile provenance label, after the drawer and identity heading were visible. Independent read-only triage found the corresponding profile-summary request eventually returned HTTP 200 in 13,200 ms, just after teardown began; the retry's request returned HTTP 200 in 882 ms. Other local routes in the same window were also slow. This supports a CI request-timing problem but does not establish its root cause. The failed desktop trace was not retained because the workflow ultimately passed; the available reports had been overwritten by later browser stages. PR #534 changed none of the drawer, profile API/data path, fixture or runner code, and no material regression from that change was demonstrated. No speculative product change, longer timeout or manual rerun was used to hide the failure.

**Final-release native compatibility:** version **322 at 13:55:09.390568 UTC** was the first normal observer poll after the final deployment. Version **323 at 14:00:09.480398** remained clear. Both retained original enablement `1788692875608`, ten cached receipts, available observation, no coverage gap/backoff, the verified scheduled run `34123496950` at attempt 1 and no active browser incidents. No cron was invoked for acceptance. This proves compatibility and continued recovered freshness on the final runtime; it does not prove a full two-hour cadence window, hosted payment or Sentry delivery.

**Completed noon checkpoint — September 7:** direct GitHub run and attempt/job reads at 12:01 UTC still identified scheduled run `34083759366`, attempt 1, whose named browser step passed at 04:37:56–04:38:28 UTC. No new scheduled invocation was observed after the two-hour schedule changed at 05:18:53. The last completion was 7 hours 21 minutes 32 seconds old at noon, so scheduled coverage remained stale.

The native observer retained `browser_stale` in version 299 at **12:00:10.082207 UTC** and also reported `observer_unavailable`. Version 300 at **12:05:08.542417** recovered observer availability while retaining the stale-browser incident. Both preserved the original enablement, ten-entry cache, last browser proof and no coverage gap or rate-limit backoff. The eight native evaluations after monitoring deployment through noon had 299.675488–301.642980-second gaps. These are actual sequential availability and persistence receipts, not a verified transport cause, new scheduled coverage or Sentry delivery.

**Actual scheduled recovery — September 7:** the first observed invocation under the two-hour schedule request was [run 34123496950](https://github.com/reabal-n/instantmed/actions/runs/34123496950), created at **12:44:17 UTC**, `event: schedule`, attempt 1, on checkout merge `d9c0921f1b3121fdda346875b92ed26bb9bc1c84`. Its exact `request-flow-synthetic` job's named browser step passed at **12:45:06–12:45:34 UTC**. The actual completion gap from 04:38:28 was **8 hours 7 minutes 6 seconds**, well beyond the requested two hours. No manual dispatch or rerun produced this evidence.

Native version 308 at 12:45:08.948488 retained the stale incident while the check was still running. Version **309 at 12:50:08.894586** verified the new scheduled completion and cleared all active browser incidents; version **310 at 12:55:08.389768** remained clear. All retained original enablement, ten cached receipts, available observation and no coverage gap/backoff. These are actual scheduled completion, incident recovery and subsequent clear-state receipts. The noon breach remains valid history, and this single later success does not establish dependable cadence or provider alert delivery.

**Outstanding read-only observations:** read the next normal review-request outcome after **September 8 00:00 UTC (10:00 Sydney)** and evaluate the full requested two-hour cadence window **September 7 05:18:53–September 8 05:18:53 UTC** (ending **15:18:53 Sydney**). The stale-browser incident has recovered, but the measured cadence gap remains part of the full-window assessment; elapsed time, manual dispatch and reruns do not establish dependable scheduling. Read persisted/provider outcomes without invoking a patient-send route or creating scheduled tasks. Sentry provider delivery remains unverified; authenticated access previously returned 403. Hosted Stripe payment and a real patient recovery replay were not performed. The existing feature-flag retry/cache behavior remains tested, with root transport cause and measured client impact unresolved.

**Preserved boundaries:** historical profile repair and its zero-change verification remain complete, with no further repair authorization or rerun needed. Preserve plaintext, keys, existing ciphertext, concurrent edits, durable monitor history and the aggregate receipt. The March certificate remains unchanged and unsent; nine retrospective cases remain clinician-owned, two April Ads discrepancies remain historical uncertainty and the two expired Ads conversion targets remain actionable. No patient contact, clinical outcome, refund or Ads mutation was performed in this follow-up.

**Plan 2 prompt (do not run automatically):** “Execute `docs/superpowers/plans/2026-09-06-02-certificate-revenue-recovery.md` using the canonical sequence in `docs/ROADMAP.md`. Read Plan 1's release receipts and observation handoff first, refresh dated operational/commercial evidence, and work only Plan 2.”
