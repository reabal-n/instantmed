'use client'

export function ContextualSocialProof({ service, variant }: { service?: string; variant?: string }) {
  return null
}

export function ServiceStats({ service }: { service?: string }) {
  return <span className="text-muted-foreground">Trusted by thousands</span>
}
