'use server'

export async function createGuestCheckoutAction(data: {
  requestId: string
  amount: number
  metadata?: Record<string, string>
}) {
  // Stub implementation - implement as needed
  return { url: '', sessionId: '' }
}

